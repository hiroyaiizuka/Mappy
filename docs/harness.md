# ハーネスと検証手順

## 現在の範囲

AI エージェントと人間が同じ条件で開発・検証するため、自動検査、テスト用データ、専用 Vault、Obsidian 非依存のブラウザ検証ページ、成果物一致の確認、実装時の規約を用意している。製品 UI は試作段階で、Markdown parser・保存・レイアウト・DOM 操作の回帰テストを実装済み。専用 Obsidian 環境での確認を進めている。

件数・バンドルサイズ・実機の PASS/FAIL をこの文書に固定せず、実行日時とビルドのハッシュを付けた `artifacts/` の記録で追う。GitHub 上の CI、ネイティブ IME、モバイル、性能計測、長時間利用の検証は、個別の証跡が揃うまで未完了として扱う。

| ゲート | コマンド / 設定 | 検出するもの |
| --- | --- | --- |
| メタデータ | `npm run validate` | manifest/package/lock/versions の不整合、必須文書の欠落 |
| lint | `npm run lint` | 製品ソースの公式 Obsidian 推奨ルール、型情報付き ESLint、JSON 構文 |
| 型検査 | `npm run typecheck` | strict、未処理の戻り、添字の undefined、型不整合 |
| 単体・DOM テスト | `npm test` | ハーネス検証器、Markdown と原文差分、保存・競合、配置・ズーム、操作 |
| production bundle | `npm run build` | ブラウザ互換 CJS バンドル。Obsidian 提供 API は external |
| 配布物 | `npm run package` | `dist/mappy/` の必要ファイルと元ビルドとの一致 |
| Vault 初期準備 | `npm run harness:prepare` | `check` 後、生成専用 Vault に配布物・fixture を配置。既知の fixture を初期化する。有効プラグインは mappy と、すでに有効なら Excalidraw（M6 用）だけを残す |
| 実機前確認 | `npm run harness:preflight` | root / dist / 検証 Vault の SHA256、一致する ID/version、有効プラグイン（mappy と Excalidraw（M6 用）だけを有効にする。他のプラグインが有効なら失敗） |
| ブラウザ検証ページ | `npm run harness:browser` | Obsidian なしで製品の map view を動かす。表示崩れ、ポインター操作、ズーム、ペインサイズ（下記②） |
| ブラウザ撮影 | `npm run harness:browser:capture` | headless Chrome で fixture 表示と主要操作を実行し、スクリーンショットと時刻を `artifacts/browser-harness/` に記録 |

まとめて実行するコマンドは `npm run check`（ブラウザ検証ページのビルドまで含む。撮影は含まない）。ローカルと GitHub Actions で同じコマンドを使う。ブランチと PR の CI（`check.yml`）は成果物を artifact に保存するだけで、公開を行わない。`manifest.version` と同じタグを push したときだけ `release.yml` が同じ check を通し、配布物 3 ファイルを GitHub Release に添付する（[リリース手順](#リリース手順)）。Git hook は任意の `npm run hooks:install` で有効にする。

## lint の対象を明確にする

`eslint-plugin-obsidianmd` の recommended は `src/**/*.ts` に適用する。推奨ルールを手書きでコピーしない。CLI やテスト準備スクリプトは Node 上で動くため別の ESLint 設定にし、モバイル禁止ルールを製品コードから外さない。

`manifest.json` は JSON lint と独立した `validate-release.mjs` の両方で検証する。設定に manifest ルール名があるだけでは、対象ファイルへ適用されている保証にならない。今回の独立検証をテストで確認する。LICENSE は存在を確認するが、公開ライセンスの適否を自動判定するものではない。

`--max-warnings 0` はこのプロジェクトの品質基準。lint の通過は、コミュニティの公開審査通過を保証しない。[公式 lint](https://github.com/obsidianmd/eslint-plugin)

Obsidian API 型は、現在の lint パッケージの peer dependency と初期最小対応バージョンに合わせ `1.8.7` を固定した。新 API の採用時は API 型、manifest.minAppVersion、versions.json、実機の互換性を一緒に更新する。ESLint 9 は現公式 sample と plugin の互換系列を採用している。依存更新では警告・サポート状況を確認し、単に最新 major へ飛ばさない。

## 既存 TaskChute Plus から引き継いだ点

参考元: 本人の Vault にある TaskChute Plus プラグイン（ローカルのみ。公開リポジトリではない）。

| 参考ファイル | 採用した考え方 | Mappy での変更 |
| --- | --- | --- |
| `AGENTS.md` | 実装後に lint・test・build、実機検証 | 一つの check と段階ごとの受入条件に集約 |
| `eslint.config.mjs` | Obsidian と型情報を使う lint | 現行 recommended を読み込み、ルール一覧の手書き複製を避ける |
| `jest.config.js` / Obsidian mock | ロジックと DOM をアプリなしで検証 | 新規プロジェクトは Vitest。実機との境界を明示 |
| `.husky/pre-commit` | commit 前の品質ゲート | 個人の Node 絶対パスを使わない任意 hook |
| `.kiro/steering/` | 仕様と受入条件を文書化 | `docs/` と短い AGENTS.md にまとめる |
| 外側の `Evergreens/.agents/skills/obsidian-e2e-tester/` | 起動前の配布物一致・ケース別証跡 | 専用 test-vault と SHA256。既存アプリの終了や他プラグイン設定を変更しない |

既存ソースは調査のみ。既存プラグインの型チェック不足、manifest が lint 対象外になる設定、公開後のビルドなどは新規構成へ持ち込まない。

## テスト階層

### 自動ロジックテスト

- Markdown: 本文/frontmatter/コメント/リンク/画像/空行/改行コードの保全、同名ノード、コード中の `#` / `-`、Setext、従来形式の深さ飛び、リストの2/4スペース・タブと深い入れ子。
- コマンド: 改名/挿入/移動/削除で対象範囲だけ変化。子孫と継続本文を含む移動、従来見出しの六段階上限、深いリスト、旧形式の明示変換、原文不一致時の拒否。
- 保存: 未保存エディタの優先、Vault.process 内の原文照合、外部更新、複数ビュー、Undo/Redo と履歴破棄。
- フリートピック: 両形式で最初の見出し区画が本体・後ろの区画がトピックになり本体の原文範囲が変わらないこと、`mappy-topics` の flow／block 読み取りと引用符付きキー、そのキーだけの差し替えと frontmatter の新規作成、改名時のキー更新、複数 H2 の fixture の表示（jsdom で製品の view を起動）。`add-topic` の文書末尾への追加（両形式、末尾改行の流儀、ヘッダーなし・リストだけの文書、コードフェンスでの拒否）、`rename` に位置を添えた 1 組の差分、削除時の項目除去（同名・非トピック・最後の項目でのキー削除、追加→削除の往復）。jsdom で製品の view を起動し、空白のダブルクリック→入力→Enter／Escape、右クリックメニュー、トピックのポインタードラッグ（両レイアウト、Escape・キャンバス外での取り消し、位置未設定・Markdown 側で改名したトピック、見出し形式）、Delete→Undo→Redo、ノードの上での合流（仮ノード・`is-merging`・区画→枝・項目の除去・Undo）、ルートの矩形による snap（4 レイアウトとも: 葉の隣は末尾の子、子の列／段は位置で前後、遠ければなし、表示中のスロットの粘り、本体と自分の木は対象外。階層図は本体直下の段の左右と葉の真下、タイムラインは軸上の左右と子のないステージの森の側（偶数番目は上だけ、奇数番目は下だけ。zone は段（軸）の最も高いノードを基準にした帯の端から測り、画像で高くなったステージの隣の低いステージでも、子が実際に置かれる位置にルートを運べば入る。jsdom は `offsetHeight` を差し替えて第一ステージを 200px にする）、左右バランスは左側の葉の左隣（右隣は何もない）・左側の子の列は右辺の線・ルートの子は右列と左列それぞれの線で前後（子の前はその子の index、列の末尾は次の index がその側に配られるときだけ全体の末尾の後ろ、反対側の末尾は何もない、子 1 つのルートの空いた左側はルートの隣）。自分の仮ノードで段が中央揃えし直されても、ステージが森を避けて右へ跳んでも、表示中のスロットを保つ。通常マップ・左右バランスの子のない木のルート（本体ルート、見出しだけのトピック）は、最初の子が置かれる右辺の 80 単位先（枝の 56 より遠く、枝の zone の 72 の外）に運んでも入り、96 先まで届いて 97 は外、左側は何もない。左右バランスで子が 1 つのルートの空いた左側（2 つ目の子が置かれる側）も左辺の 80 先で入り、97 は外。view が通常マップでもルートに `"root"` を渡すことは jsdom で固定する（LEV-90）。左右バランス・階層図で位置未設定のトピック（先頭は見出しだけ、末尾にもう 1 つ）の文書を開き、`shiftTopic` で後ろのトピックを先頭のルートの最初の子が置かれる位置（右辺の 80 先／下辺の 48 下）へ運んで `snapTarget` → `previewDrop` すると、先頭のルートの配置位置が事前表示の前後で同じで、仮ノードは運んだルートの位置に置かれ、末尾のトピックも動かないこと。本体の枝を見出しだけのトピックへ `previewDrop` した（トピックのドラッグなしの）事前表示でも同じこと。通常マップで子 2 つの位置未設定のトピックの末尾へ事前表示しても親のルートが動かず、森だけがルートの高さ中央に揃い直すこと（LEV-95。仮ノードで広がった木を中央揃えし直したり運んだ木を避けて積み直したりしない）。`snapSlot` の境界値・森の始まる列による順位・帯の端からの zone とステージ自身の辺までの届き・実際の配置結果（`layoutTree` に子を足した位置）が zone に入ること（ルートの 80 先、枝の 56 先、階層図・タイムラインのルートの 48・32 先を含む）は layout のテストで固定する）、本体のドラッグ（viewport・全トピックの項目・Escape での復元）、枝を空白へ離しての切り離し（区画化・離した位置・押した場所の近くでは何もしない・Undo）で原文と `mappy-topics` を確認する。`detach` の枝→区画の変換（深い枝、トピック内の枝、仮想ルート直下の項目、末尾の項目、CRLF、フェンス、タブ、同名トピックとの衝突、見出し形式）も core で固定する。`move` の区画→枝の変換（兄弟のインデント・マーカー、子なし、末尾区画、CRLF、コードフェンス、見出し形式）と `planTopicMoves` は core のテストで固定する。
- レイアウト: 非重複、順序、枝の大きさ、折りたたみ、上下交互タイムライン、線がノード内や下線へ伸びないこと。左右バランスの振り分け（第一階層が原文順に右・左・右・左、下位はその側。右列と左列を交互に読むと原文順に戻る）・各側の列がルートの高さ中央に揃うこと・左側の鏡像（子の右辺の揃え、親の左辺から子の右辺への直角線、左の幹の開閉ボタンと左のバッジ）・片側の折りたたみで反対側が動かないこと・再展開で元に戻ること・4 桁バッジ・両側 2,000 段の一列・500 ノード混在・`uneven-branches` と 10／100／500／2,000 ノードの fixture・フリートピック（位置ありは origin＋位置、位置なしはルート中央の列）・Excalidraw 挿入との座標一致。階層図の段揃え（子の上辺 = 自分の親の下辺 + 隙間。兄弟は同じ y、画像付きの親の子だけが下がる）・原文順・親の中央揃え・直角線が段の間だけを通ること・開閉ボタンと 4 桁バッジの位置、`uneven-branches` と 10／100／500／2,000 ノードの fixture での非重複。フリートピックの指定位置（本体ルート基準）と既定配置（本体の下、重ならない最初の空き）、Fit の bounds。
- ズーム: ポインター下のワールド座標が一定、倍率上限、パン、Fit。
- 設定（M14）: `normalizeSettings` が欠損・null・旧形式・不正値を項目ごとに既定値へ戻し、未知のキーを捨てること。設定タブ（jsdom、ブラウザ検証ページのモックの `Setting`／`DropdownComponent`／`TextComponent`／`ToggleComponent`／`PluginSettingTab`）が 4 項目だけを出し、レイアウトの選択肢が `LAYOUT_MODES` の順であること、変更した項目だけを差し替えて保存し、Vault・frontmatter に一切書かないこと、1.13 以降向けの `getSettingDefinitions`／`getControlValue`／`setControlValue` が `display()` と同じ 4 項目を返し不正値を保存しないこと。`createMindmapFile` が既定の設定で従来と同じ frontmatter・作成先になり、既定レイアウトを新規ノートだけに書き、作成先フォルダを正規化して既存なら（大文字小文字違いでも）再利用・なければ作成・同名ファイルと `.` で始まる名前なら拒否・作成結果が空ならエラーにすること（モックの `normalizePath` は Obsidian と同じくスラッシュしか整えない）。1.13 以降の `addSettingTab → update() → 描画` の流れをモックで再現し、`settingItems` が 4 件になり `display()` に落ちないこと（モックの `renderTab()` は宣言的な行——`dropdown`／`text`／`toggle` の binding と `render` の行——を描き、`hide()` で `render` の cleanup を呼ぶ）。左下に表示するレイアウト（LEV-76）: `readVisibleLayouts`／`normalizeSettings` が未知の値・重複を落とし `mindmap` を必ず含めて `LAYOUT_MODES` 順に揃え、配列でなければ 4 つすべてに戻すこと。設定タブの行がトグル 4 つで、通常マップが on 固定・操作不可（クリックしても保存しない）、他のトグルの on／off（文字ラベルのクリックでも）が正規化した配列で保存され、保存に失敗したらトグルが戻って次の変更が保存済みの値から計算されること、既定レイアウトが非表示のときだけ 1 行の注記が出て、トグルと既定レイアウトのどちらを変えても更新され、隠したあとの保存では外れた要素に触れないこと、`display()` と 1.13 の `render` の両経路で同じになり、1.13 の `update()` → 再描画で行が重複しないこと、`SettingTab`／`PluginSettingTab` の名前（`update`・`hide`・`renderTab`・`getControlBinding`・`refreshDomState`・`getElementForDefinition`・`getDefinitionForElement`、フィールドの `settingItems`・`renderedItems`・`setting`・`navEl`・`name`・`id`・`icon`）をこのクラスが使っていないこと。`MindmapView.setVisibleLayouts()` が既定では `hidden` を付けず従来と同じ DOM、設定外のボタンだけを `hidden` にし、開く前の設定が `onOpen` 後も残り、表示中のレイアウト（frontmatter・view state・ボタン選択）のボタンは非表示でも残って別のレイアウトを選ぶと消え、ノートのない view や読み込み前でも `mode` の変更に従い、原文・frontmatter・view state・別 view に影響しないこと（view の起動は `tests/ui/map-view-mount.ts` の `prepare` で設定を先に渡す）。`readPreferredMapLayout` が旧 `mappy-layout` を持つノートではその値、持たないノートでは設定の既定を返し、`readMapLayout`（既存ノートの表示）は設定を見ないこと。`MindmapView.setTheme()` がコンテナにだけ `theme-light`／`theme-dark` を付け外しし（body・leaf・canvas・別 view には付かない）、開く前に設定した値が `onOpen` 後も残り、「Obsidian に従う」で両方外れ、原文・frontmatter・レイアウト保存が起きないこと。styles.css のテーマ変数が `:where(.mappy-view.theme-*)` にだけあり、カスタムプロパティ以外を含まないこと。その変数に `--caret-color: var(--text-normal)` があり、`.mappy-view` が `color` と並べて `caret-color: var(--caret-color)` を読み直し、styles.css 全体でプロパティ `caret-color` の宣言がその 1 つだけであること、`:where(.is-mobile .mappy-view.theme-*)` が `--caret-color: var(--text-accent)` だけを持ち theme 上書きより後にあること（LEV-93。app.css は body で変数とプロパティの両方を確定させる）。
- SVG／PNG 書き出し（M13）: 純粋部分（`tests/export/svg-document.test.ts`）は viewBox と余白、PNG の縮尺の上限（面積・一辺・希望倍率）、XML のエスケープ、スタイルの重複除去、シーン → SVG の構造（背景 → 線 → ノード → バッジの順、CDATA の終端）。DOM 部分（`tests/export/svg-capture.test.ts`、jsdom で製品の view を起動）は `foreignObject` の数と id が表示ノードと一致し座標が `LayoutResult` と一致すること、線の数、viewBox、テーマ class と `data-theme`（canvas から最も近い `.theme-light`／`.theme-dark`、無ければ body の `theme-dark`。`setTheme('light')` のマップを暗色の body で、`setTheme('dark')` を明色の body で書き出すとマップのテーマになり、`follow` で body に戻ること。LEV-92）と既定の背景色、状態クラス・開閉ボタン・`tabindex` を含まないこと、折りたたみ（枝が消え、件数バッジが `folds` の位置に出る。配置フレームが予約中でも `exportSource()` が待ってから返す）、欠落・読めない画像でノードが残り読める画像は data URL になること、タイムライン・階層図、編集中と file なしの拒否、10／100／500／2,000 ノードの完了と原文不変、画像を待つ間に DOM が壊されてもノードが欠けないこと、debounce 中の編集が書き出しに入ること、外部変更の直後で `MarkdownRenderer.render` の解決を遅らせたモックでも描画の完了と再計測後のサイズを待ってから返すこと（LEV-65）、debounce が発火して `vault.read` が遅い最中の書き出しでもその refresh の結果が入ること、解決しない描画は `EXPORT_RENDER_WAIT_MS`（2 秒。fake timers）で諦めて Notice を出し画面のまま返すこと（`NodeRenderer.idle(stall)` 自体は `tests/ui/node-renderer.test.ts`: 描画なしで即解決、全描画の完了後に `changed()` より後で解決、描画が続いている間は 2 秒を超えても待ち 1 件も終わらない 2 秒で `false`、再描画・削除で置き換わった描画とその遅い完了を待たない、削除で待ちが終わるときも `changed()` を呼ぶ、失敗した描画と unload で終わる）、フォームフィードと `xlink:href` 付きのインライン SVG でも整形式であること、汚染された canvas が「PNG を作れません」になり probe が false を返すこと。Obsidian 側（`tests/obsidian/image-export.test.ts`）は添付パス API の有無による可否、resolver（data URL の素通し、埋め込みの link target を `readBinary`、Markdown 画像の書かれたパス、http(s) の取得と失敗時の null、PDF を読まない、`image/*` 以外の応答と応答のないホストの拒否）、`getAvailablePathForAttachment` → `vault.create` での保存とノート不変、整形式の検査、canvas のない環境で添付パスを取る前に PNG を断ること。モーダル（`tests/ui/export-modal.test.ts`）は形式の選択と PNG の無効化。
- マップの中の呼び出し（M12 の表示側）: core で「項目の文が `![[…]]` 1 つだけか」（`embedOnlyTitle`: 前後の空白、`|別名` の切り捨て、文中・2 つ・コード・空は null）と投影（`tests/core/calls.test.ts`: 項目が呼び出し先のルートの文を持ち、子が「呼び出し先のルートの子 … 自分の子」の順で、ホストのノードは id・範囲・種別を保ち、呼び出したノードは `項目の id/ノードの id` で親と深さを付け直し原文範囲は呼び出し先のまま、出所 `CallSource`（項目は `root: true`）、`#見出し` はその区画だけ、同じマップの 2 回は別の id、対象なし・文が変わった・見出しなしは素通り、呼び出し先の中の `![[…]]` は複製されるだけで継ぎ足されない（1 段）、仮想ルートの本体と空の区画、フリートピックのルート、初期の折りたたみは呼び出し先のルート以外の子を持つノード）。読み取り（`tests/obsidian/map-calls.test.ts`）は `![[…]]` だけの項目をマップノートに解決し（自分自身・`mappy: true` のないノート・存在しないノート・ブロック参照は載らない）、同じノートは 1 度だけ解析、原文が同じなら同じ文書、編集で id が保たれ、原文で `mappy: true` を失えばリンク、読めなければリンク、重なる読み取りは逐次。jsdom で製品の view を起動し（`tests/ui/called-branches.test.ts`）、`![[Map]]`／`![[Timeline]]`／`![[Headings#同じ名前]]` の項目が呼び出し先のルートの文（`is-called-root`、`link` の印、読み上げの説明（`aria-describedby`）に元ノートのパス。`aria-readonly` は呼び出したノードだけ）になりその子が枝として並ぶこと（それより下は件数バッジで折りたたみ、呼び出し先のフリートピックは描かない）、4 レイアウトでその枝が現在のレイアウトの class を持つこと、自分自身・文中の埋め込み・`mappy: true` のないノート・存在しないノート・ブロック参照・インラインコードはリンク／文、画像は画像のままであること、同じマップの 2 回の呼び出しが別の id でそれぞれ独立に開閉すること、A→B→A・A→B→C→A で呼び出した枝の中の `![[…]]` がリンクのままであること（B 側から開いても同じ）、呼び出したノードのクリックで選択され Space と開閉ボタンで折りたたみ矢印キーで枝の中を辿れること、Enter／Tab／Delete／Backspace／F2／⌥↑／画像のドロップが Notice「読み取り専用」で断られ原文が変わらないこと、呼び出したノードからドラッグが始まらず呼び出したノードへのドロップにスロットが出ないこと、呼び出したどのノード（項目も）のダブルクリックでも元ノートを開く要求になり（⌘ で新しいペイン）インライン入力が開かないこと、右クリックが「元のマップを開く」「折りたたみ」と履歴だけになること（項目は先頭に「元のマップを開く」を足した通常のメニュー）、項目の F2 が原文 `![[…]]` をそのまま編集し Tab の自分の子が呼び出した枝の後ろに並び Delete で木ごと消え ⌘Z で戻ること、改名で別のマップ（ルートの子まで開く）やただの文に変わること、呼び出したノードの下への `callMap` が断られ項目の下には呼べること、元ノートの保存・`editor-change` で枝が更新され折りたたみが残り新しい枝は折りたたまれホストが変わらないこと、`mappy: true` が外れるとリンクに戻ること、呼び出し先が後から作られる・`mappy: true` を得る・消える（metadataCache の `changed`／`deleted`）とホストを編集しなくても枝になり／リンクに戻り無関係なノートの変更では描き直さないこと、2,000 ノード相当のマップがルートの子までで始まり一段ずつ開くこと、SVG の capture とシーン（`sceneContents`）に呼び出したノードが通常のノードとして入ること、view の閉じで vault／workspace の購読が 0 になること、`## ![[Map]]` のトピックのドラッグが `mappy-topics` を原文の見出しでキーにしその位置で配置されること、呼び出したノードの中のリンクが呼び出し先を基準に・項目自身の本文のリンクがホストを基準に開くこと、項目の下へのドロップの事前表示が呼び出した枝の後ろに置かれること、呼び出したノードを選んだ「Markdown に切り替え」が項目の位置にカーソルを置くこと、無関係なノートの変更で呼び出し先を読み直さないこと。SVG 書き出し（`svg-capture.test.ts`）は呼び出した枝を通常のノード（`is-called`、`link` の印、折りたたみのバッジ）として出す。`nodeOf`／`mapClick`（`map-events.test.ts`）は入れ子を持たず、ダブルクリックが `open` に先に問い、矢印が画面の木で辿ることを固定する。`NodeDrag`（`node-drag.test.ts`）は `readOnly` のノードから押下を始めないことを固定する。`NodeRenderer`（`node-renderer.test.ts`）は `sources` のノードを呼び出し先の文書・パスで描き、class・読み上げの名前と説明（`aria-labelledby`／`aria-describedby`。ノード要素に `aria-label`・`title` を置かないこと。LEV-199）・印・key（パスと `called-root`）と、呼び出しが消えたときリンクに戻ることを固定する。Excalidraw（`excalidraw-scene.test.ts`・`excalidraw-bridge.test.ts`）は継ぎ足したシーン、項目の要素の呼び出し先へのリンク、呼び出したノードのリンクと画像の呼び出し先基準の解決、`calls` なしの要求（Markdown view）で bridge が自ら読み初期の折りたたみで入れること、非マップの呼び出しがリンクのままであることを固定する。
- 埋め込み（M10）: core で原文からのマップ識別（`mappy: true` の真偽値だけ。`True`／`TRUE` も cache と同じく真偽値、`"true"`・Excalidraw・未閉の frontmatter は対象外）、`#見出し` の区画解決（文書順の最初の一致、`#A#B` の入れ子、大文字小文字・空白・`:#|^` の正規化、リスト項目は対象外、ブロック参照と見つからない見出しは null）、描く木（全体は本体＋フリートピック、見出しは部分木だけ）、開いた時点の折りたたみ（ルート直下より下の全枝）、タイトルの `![[ノート]]` のリンク化。jsdom で製品の post-processor を Obsidian 風の区画に通し、閲覧モード経路（placeholder の差し替え、Obsidian が先に読み込んだ span は claim、対象外の素通し、部分木と見つからない見出しの文言、`mappy-layout` とフリートピック、原文不変、保存と別 leaf の未保存編集での再描画、読者の折りたたみの保持（一時的な文言をはさんでも）、マップでなくなった場合の文言、区画の unload でのリスナー解放、プラグイン unload での placeholder の復元とその枠を含む閲覧モードの view だけの描き直し、「マップで開く」とノード内リンクの基準、Fit の上限 1 倍、自己埋め込みとタイトル経由の循環がないこと）とライブプレビュー経路（容器の claim が 1 度だけ、区画の中の anchor が寿命を持ち枠は容器に付くこと、Obsidian のクラスの退避と復元、自分のノートの閲覧モードは対象外、通常ノートの埋め込みの中のマップは描き claim した容器の中の placeholder は描かない、未接続の区画の見直しと unload 後の拒否）を確認する。
- マップの検索と呼び出し（M12 の入力側）: core（`tests/core/list-commands.test.ts`・`commands.test.ts`）で `add-child` に `title` を添えたとき、子のあるノードではその最後の子の枝の直後に同じインデント・マーカーで、葉では 1 段深く、本体ルートと子のない区画では add-child と同じ位置に 1 行の項目が入り、選択はその項目の文、同じ文を 2 回足すと 2 項目、改行を含む文と項目にならない文（`[ ] task`）は拒否、文書末尾では末尾改行の流儀（あり・なし）を保つこと（最後の H2 の兄弟追加と仮想ルートへの子追加も同じ）、見出し形式では 1 段深い見出しになり CRLF を保つこと。モーダル（`tests/obsidian/map-search.test.ts`、ブラウザ検証ページのモックの `FuzzySuggestModal`）は候補が `mappy: true` の真偽値を持つ他の Markdown ノートだけ（`"true"`・Excalidraw・`.png`・自分自身は出ない）でパス順、検索文字列が拡張子なしのパス、各候補がファイル名と親フォルダ（最上位はファイル名だけ）、プレースホルダーとキーの案内、候補 0 件の「マップがありません」と検索語に一致しないときの「一致するマップがありません」、タイトルと `フォルダ/タイトル` での絞り込みと一致箇所の `suggestion-highlight`、クリックと Enter で 1 度だけ返して閉じること。view（`tests/ui/mindmap-view-call.test.ts`、jsdom で製品の view を起動）は `callMap` が未選択なら本体ルートの子の末尾、選択ノードならその子の末尾（葉は 1 段深く）、フリートピックならそのトピックの子の末尾に `- ![[別マップ]]` を書き、追加した項目が選択されインライン入力は開かず、呼び出したノートは不変、Undo 1 回で項目ごと消えて Redo で戻ること、同じマップを 2 回呼ぶと 2 項目（続けて呼ぶと選択が新しい項目に移っているのでその下に入る）、見出し形式では `### ![[別マップ]]`、自分自身・インライン編集中（右クリックの子追加も `execute` で同じ）・保存中・H2 のない文書の拒否で原文が変わらないこと。Obsidian のモーダルの見た目・`fileToLinktext` の実際の出力・ホットキーは③で見る（ブラウザ検証ページにはこのモーダルを載せていない）。
- ハーネス: `tests/tooling/preflight.test.mjs` が一時ディレクトリに配布物と生成 Vault を組み立て、`community-plugins.json` が `["mappy"]`（生成直後）と `["mappy", "obsidian-excalidraw-plugin"]`（M6 の Vault）なら preflight が通り、他のプラグインが有効・mappy が無効・プラグイン ID の配列でない内容なら失敗すること、CLI の終了コードと表示、`prepare-test-vault.mjs` の再実行で Excalidraw が残り他のプラグインが落ちることを確認する。

Vitest の Node 環境で検証する。乱択・property-based test は、保存と復元の不変条件に効果がある場合に追加する。

### DOM とブラウザ

jsdom を導入し、製品の DOM 操作・インライン入力・キー操作を検証している。composition イベントを送るテストは変換中のキー制御を検証するもので、OS の日本語 IME で入力した結果ではない。Obsidian のキーマップ（`window` の capture 段階で active view の `Scope` → グローバルホットキーの順）は jsdom にないので、ブラウザ検証ページの `Scope` モックに Obsidian 1.14.2 の `Scope.handleKey`（登録順に照合、キー指定のハンドラは `undefined` でも探索を終える、catch-all だけが親へ降りる）と `compileModifiers`（`Mod` の変換とソート）を写し、map view が `Scope`（親 `app.scope`）に修飾キーなしの F2 だけを登録していること、view の中（キャンバスの選択ノード・inline 入力の中・浮かせたボタン）では `false`（Obsidian の「消費」）を返して inline 編集を開く／何もしないこと、view の外では `undefined` を返し root の catch-all（HotkeyManager 相当）に F2 が渡らないこと、他のキーは root に渡ること、`false` で `preventDefault`＋`stopPropagation` するキーマップ相当のリスナーを置いてもキャンバスのリスナーが二重に編集を開かないこと（`tests/ui/mindmap-view-scope.test.ts`、`tests/ui/map-events.test.ts`。view の起動は `tests/ui/map-view-mount.ts`）を固定し、実キーは ③ の CDP で確認する。DOM の実測サイズ、フォント、画像遅延、ポインター操作、ズームの見た目は、下記②のブラウザ検証ページと③の Obsidian 実機で確認する。

### ② ブラウザ検証ページ（Obsidian 非依存）

`harness/browser/` にあるページで、製品の `src/ui/mindmap-view.ts`・`node-renderer.ts`・`map-viewport.ts`・`map-events.ts`・`inline-editor.ts` と `core` / `layout` / `interaction` / `document-store.ts` をそのまま読み込む。`obsidian` モジュールだけを `harness/browser/obsidian.ts`（`tests/mocks/obsidian.ts` と同系統のモック。Component・ItemView・Scope・Menu・Modal・SuggestModal／FuzzySuggestModal（部分一致の簡易検索。Obsidian の採点は模さない）・Notice・setIcon・MarkdownRenderer の最小実装。Scope は登録を保持するだけで、キーマップはない）に差し替え、Obsidian が起動時に生やす DOM ヘルパー（`createDiv`、`addClass`、`event.targetNode` など）は `harness/browser/dom.ts` が同じ形で prototype に載せる。Vault・workspace・metadataCache・fileManager は `harness/browser/app.ts` のメモリ内実装で、ファイルへは何も書かない。

- 起動: `npm run harness:browser` で `dist/harness/` をビルド・監視し、`http://127.0.0.1:8765/` で配信する（`--port` で変更）。`npm run harness:browser:build` は一度だけビルドし、`dist/harness/index.html` を `file://` で直接開ける。ビルドは製品と同じ esbuild を使い、ランタイム依存を追加しない。
- fixture: `tests/fixtures/` の `heading-document`（従来の見出し形式・リンク・画像）、`roundtrip-edge-cases`（同名見出し・コードブロック・欠落画像）、`uneven-branches`（H2＋リスト、8 段の一列の枝、24 兄弟、長い日本語、リンク・画像・コードブロック、同名ノード）、`free-topics`（複数の H2: 本体＋フリートピック 3 つ、`mappy-topics` のレイアウト別位置・引用符付きキー・孤児キー、位置未設定の既定配置）、`embed-nodes`（`mappy: true` のマップ。`![[embed-timeline]]`・`![[embed-hierarchy#同じ名前]]`・`![[embed-2000]]`・同じ `![[embed-timeline]]` の 2 回目・`![[embed-cycle]]` だけのノードが呼び出し先のルートになってその木が枝として並び、自分自身・文中の埋め込み・`mappy: true` のないノート・存在しないノート・ブロック参照はリンク、`![[sample-image.svg]]` は画像。M12、E35）、`embed-cycle`（`embed-nodes` と互いに呼び出す。自分自身はリンク）、`timeline-stages`（`mappy-layout: timeline`。下側の深い森の右に同じ側の次のステージの縦線が立つ間隔、上側の画像つき・幅の違う枝。LEV-205、E42）と、`scripts/performance-fixtures.mjs` が生成する 10／100／500／2,000 ノード。`harness:prepare` が `test-vault/Fixtures/` に置く文書と同一で、ページ左の select か `?fixture=<id>` で切り替える。
- 埋め込み（M10、E34）: select の「埋め込み（ホストノート）」は map view ではなく、`tests/fixtures/embed-host.md`（`mappy: true` のない通常ノート）を描いた区画に製品の post-processor（`src/ui/map-embed.ts` の `MapEmbeds`）を通す。ホストは `uneven-branches`（通常マップ）、`embed-timeline`（`mappy-layout: timeline`）、`embed-hierarchy`（見出し形式・`mappy-layout: hierarchy`・同名の `### 同じ名前` が 2 つ）、`embed-hierarchy#同じ名前`（部分木）、`embed-2000`（`makeEmbedFixture()`: 均等な枝 2,000 ノードに `mappy: true` を付けた生成文書。`harness:prepare` も書く）を埋め込み、`heading-document`（`mappy: true` なし）・存在しないノート・`embed-hierarchy#^block`（ブロック参照）は Obsidian の通常の埋め込みのまま（このページは中身を描かず placeholder を出す）。`embed-host` は閲覧モード相当（ホストの区画の `.internal-embed` を差し替える経路）、`embed-host-live` はライブプレビュー相当（埋め込み先のノートを Obsidian 風の `.internal-embed.markdown-embed` 容器に描いてから、その区画を埋め込み先の sourcePath で processor に渡す経路）、`embed-host-live-late` は同じ経路を Obsidian 1.6.7 がノートを開くときの順序で行う（LEV-91: 各埋め込みを 2 回描き、1 回目の区画は processor に渡した後どこにも付けずに捨てる。2 回目の区画は容器の外で processor に渡し、1 フレーム後に容器へ入れ、さらに 2 フレーム後に容器を document に付ける。画面の下の 2 つの容器は実機のスクロールに合わせて 90 フレーム≈1.5 s 後に付ける）。`window.__mappyHarness.embeds()`（各埋め込みの種別・レイアウト・ノード・scale・文言）、`liveEmbeds()`、`pendingClaims()`（容器の接続を待っている区画の数。60 s の保持の間）、`productFrames()`（製品の rAF の回数。待ちが polling していないことの確認）、`disposeEmbeds()`（プラグイン無効化と同じ解放）、`putNote()`（元ノートの書き換え）を持ち、暗色はページのテーマ（`setPageTheme("dark")`、`?page-theme=dark`）で確認する。Obsidian の描画順序・ホバープレビュー・実テーマ・埋め込み内リンクの遷移は③で確認する。
- マップの中の呼び出し（M12、E35）: `embed-nodes`／`embed-cycle` は map view の fixture で、`![[…]]` だけの項目が呼び出し先のルートになり、その木が枝として並ぶ。`nodes()` は呼び出した枝のノードも含めて DOM 順に返し、各項目は `called`（呼び出したマップから来た）・`calledRoot`（項目そのもの。`title` は呼び出し先のルートの文）・`source`（`aria-describedby` の指す `.mappy-node-description` の元ノートのパス。LEV-199 までは `title` 属性）・`badge`（閉じた枝の件数）・`link`（自分の label の内部リンク）・`image`（自分の label の画像）・`color`（label の算出色）を持つ。`node(title, n)` は同じ文の n 番目、`nodeById(id)` は id で引く。`listeners()` はページ内 vault／workspace の購読数。`putNote()` は新しいノートも作り、`removeNote()` はノートを消す（どちらも cache の `changed`／`deleted` を出す）。`notices` は Notice の記録（読み取り専用の通知を数える）。ダブルクリックはリンク解決の要求の記録（`activity`）だけで、実際の遷移は③。
- 操作: 選択（クリック・矢印キー）、開閉（分岐点の − と Space）、パン（背景ドラッグ・ホイール）、ズーム（Ctrl/⌘＋ホイール、ピンチ、右下の −／倍率／＋／全体表示）、レイアウト切替、右クリックメニュー、右上の歯車のポップオーバー（§5 M3。`src/main.ts` が渡す呼び出し・書き出しの 2 項目は `harness/browser/main.ts` の代役で、どちらも通知だけ）、ペインのサイズ変更（プリセット、数値、右下の角のドラッグ。変更ごとに製品の `onResize()` を呼ぶ）、「閉じて開き直す」（`onClose`→`unload`→新しい view）、テーマ（「ページ」は body の `theme-light`／`theme-dark` で Obsidian の外観に相当、「マップ」は設定「テーマ」に相当し製品の `setTheme()` を呼ぶ。`?theme=light|dark|follow`、`?page-theme=dark` で初期値を指定。`window.__mappyHarness.setPageTheme()`／`setMapTheme()`／`themes()`）、左下に表示するレイアウト（設定「左下に表示するレイアウト」に相当する 4 つのチェックボックス。通常マップは固定。製品の `setVisibleLayouts()` を呼び、`window.__mappyHarness.setVisibleLayouts([...])`／`layoutButtons()` で操作と読み取り。設定タブそのものはこのページにない）。Enter／Tab／F2／Delete と右クリックの編集、フリートピックの追加（空白のダブルクリック・右クリック）・ドラッグ移動・削除はメモリ内の文書に対して動き、`window.__mappyHarness.source()` で現在の原文を読めるが、保存経路の検証ではない。
- 時刻の記録: fixture 切替ごとに `parseMarkdown` 単体、`setState` 完了、最初の描画フレーム、位置が 3 フレーム安定するまでの経過 ms をページ左の一覧と `window.__mappyHarness.timings`、Performance タイムラインの `mappy:load:<id>` に残す。1 回分の生値で、繰り返しと p50／p95 は下記の性能計測が扱う。
- 性能計測: `node scripts/browser-harness-perf.mjs` は同じページを headless Chrome で開き、`scripts/performance-fixtures.mjs` の 10／100／500／2,000 ノード × 6 つの形（見出し形式、H2＋リストの均等な枝、深い一列の枝、多数の兄弟、長い日本語、リンク・画像）を、マップ・タイムライン・階層図・左右バランスの 4 レイアウトで計測する。fixture ごとに新しい Chrome を起動し、レイアウトごとに、新しい view での読み込み（`parse`・`setState`・ノードの計測・`layoutTree`・配置フレーム・次フレーム開始・初回配置・安定）10 回、Markdown 側の編集（`modify` → 45 ms の debounce → 再読込〜DOM → 配置フレーム → 次フレーム開始）10 回、インライン編集（キー入力 30 回と Enter の確定 10 回）、パン／ズーム各 2 回 × 60 フレーム（1 フレームに 1 wheel）、フリートピックのドラッグ 2 回 × 60 回のポインター移動（1 フレームに 1 移動。LEV-126）を `harness/browser/measure.ts` の探針（製品の requestAnimationFrame と setTimeout を包み、編集ノードの MutationObserver で DOM 更新の時刻を取る。`src/` は触らない）で取り、nearest-rank の p50／p95 と基準端末（CPU・メモリ・OS・Node・Chrome・フラグ・load average・build）を `artifacts/performance/<日時>/record.md`（`samples.json`・`summary.json` も）に書く。`--counts`・`--shapes`・`--fixtures`・`--layouts`・`--repeat`・`--keystrokes`・`--frames` で範囲を絞り、`--gpu` で `--disable-gpu` を外して GPU ラスタで比較する。画像は data URL で転送時間を含まない。テーマは `harness.css` の仮の値で、Obsidian の Editor と Vault を持たないため、実機の値（E10、`artifacts/lev-33-realtime-verification-2026-09-18.md`）と突き合わせて読む。ドラッグは、本体と同じ深さの区画を 1 つ足して 1 トピックのマップにし、「全体表示」で Fit したあと、Fit が残す空白の帯で運んだときの pointermove 1 回の同期時間（運ぶ木の位置更新と `snapTarget` の探索）、その移動が予約した配置フレーム、rAF の間隔を記録する。Escape で終えるので原文は書き換わらず、足した区画は計測後に取り除く。
- 自動撮影: `npm run harness:browser:capture` は headless Chrome（`--chrome <path>` か `MAPPY_CHROME`、既定はインストール済みの Google Chrome）を DevTools Protocol で操作し、全 fixture の Fit 表示と、`uneven-branches` での選択・矢印キー・開閉・パン・ホイール・Ctrl＋ホイール・ズームボタン・タイムライン・階層図（親ごとの段揃えと深さごとに一定の隙間、`mappy-layout: hierarchy` の書き込み（レイアウトの書き込みは `h.layoutKey()` でノートの原文を製品の読み方（`frontmatterReader`・`readMapFromSource`）で読む。読む前に view の `layoutWritten()` でボタンが積んだ書き込みの完了を待つ。検証ページの `processFrontMatter` は `h.activity` に記録するだけで原文を書き換えないので、その記録では判定しない。書かれないことは、その完了のあとで原文が変わっていないことで見る。`uneven-branches` で始まるグループ（操作・改行・テーマ・左下に表示するレイアウト・書き出し）と `heading-document` の階層図の段は、別の fixture を開いてから `h.restoreFixture()` で出荷時の原文に戻し、通常マップで開き直して始める。LEV-212）、折りたたみの件数）・タイムラインの同じ側のステージの間隔（`timeline-stage-gap`: `timeline-stages` の Section2 の森の右端から Section4 の縦線までを view と Excalidraw に挿入する scene の両方で測り、`TIMELINE_STAGE_CLEARANCE` と一致。LEV-205）・640×480／390×700 のサイズ変更・右クリックメニュー・右上の歯車のポップオーバー（`action-popover`: ボタンが 1 つ、カードが歯車の直下に右辺を揃えて開く、3 項目とアイコン、3 行の左端・幅・アイコンの左端が同じ（LEV-84）、1 項目目にフォーカス、↓で移動、Escape でキャンバスへ、2 度目の押下で閉じ 3 度目で開く、外側クリック。`action-popover-narrow`: 400×700 のペインで左辺がペイン内、3 行の左端・幅・アイコンの左端が同じ）・内部リンククリック・F2 入力→Enter→⌘Z（メモリ内の改名と Undo）・閉じて開き直し、`performance-2000` の開閉、左右バランス（`balanced`: 第一階層が原文順に右・左・右・左、下位はその側、各側の列がルートの高さ中央、`mappy-layout: balanced` の書き込み。`balanced-collapse`: 左側の「多数の兄弟」の開閉ボタンが左の幹にあり、閉じると件数 24 がノードの左に出て右側の枝は動かない。`balanced-scene`: `window.__mappyHarness.scene()` で組み立てた Excalidraw 挿入のシーンの各ブロックが画面上のノードとルート基準で一致し、線がすべて直角。`fold-2000-balanced`: `performance-2000` を左右バランスで開き、100 節が右・左 50 ずつ、左側の「第2節」を閉じて 19 ノードが隠れ再展開で戻る）、`heading-document` を階層図にして親ごとの段（`hierarchy-rows`: 兄弟は同じ上辺、画像付きの親の子だけが下がり、文字だけの枝の隙間は画像付きの親の子と同じ長さ。`mappy: true` の無いノートなので、階層図のボタンでも `hierarchy-rows-back` で「通常マップ」に戻しても原文は変わらない（LEV-196 の `planMapLayout` はマップでないノートに書かない）。LEV-212）、インライン入力の幅（`inline-width-*`、LEV-198: F2・ダブルクリック・Enter／Tab の新しいノード（LEV-203 から「サブトピック」が全選択で開き、入力欄はノードの文字の枠と同じ幅）× 第一階層・深い階層・ルート・フリートピックのルート・画像を持つノード × 全角・半角・混在・URL × 4 レイアウト、`field-sizing` を無効にした測る経路。1 文字ずつ打って横へ広がり、ノードの上限から余白を引いた幅で 2 行目になる位置を測り、その直前・直後・全文を原文から入力し直して確定し、ラベルの行数が入力中と同じこと。確定後の長い題名のノードが 420px・ルートの上限が 360px のままであること。画像を持つノードでは 1 文字の入力欄がノードの幅いっぱいで右端の押下が入力欄に当たること。左右バランスの左側は親側の辺が動かないこと。このページの外部リンクには Obsidian のアイコンが付かないので、URL の一致は実機の一致ではない）、新しいノードの入力欄（`new-node-*`、LEV-203: 4 レイアウト × `free-topics` の通常ノード・本文のルート・トピック・ステージ × 空・1 文字・折り返す長さ・2 行（Shift+Enter）で、F2 → 入力 → Enter → F2 → Escape の各時点のノードの外形（配置位置と倍率 1 の幅・高さ）が 0.5px 以内で同じで、入力欄が行数ちょうどの高さでノードの文字の枠の上端から始まること。Tab の新しいノードが「サブトピック」を全選択で開き確定後も同じ外形、IME の変換開始（`Input.imeSetComposition`）が仮の名前を置き換えること、すぐの Escape で 4 レイアウトとも原文が元のままになり元のノードが選択に戻り、右クリックの「元に戻す」「やり直す」がどちらも無効なこと（取り消しを Undo で作り直すと「やり直す」が有効になる。1 回目の修正からの実装では通る——これは作り直しを捕まえるための検査）、画像だけを持つ題名が空のノードに空の行も 96px の最小幅も付かないこと（`new-node-image-only`）。`new-node-ime` は修正前のビルドでも通る——置き換える仮の名前がそもそも無いため。固定しているのは「変換の開始が全選択の仮の名前を置き換える」ことだけ）、`uneven-branches` でテーマ（`theme-follow-light`／`theme-dark-on-light`（開き直し後も保持）／`theme-light-on-dark`／`theme-follow-dark`／`theme-back`: コンテナの class と、キャンバス背景・ノード文字・リンクの computed color、`color-scheme`、ページ背景が仮の配色どおりに切り替わること。`theme-back` 以外は F2 でインライン入力を開き、その `caret-color` と `::selection` の背景がマップ側の文字色・選択色になること（LEV-93。harness.css は app.css と同じく body で `caret-color: var(--caret-color)` を確定させる。`theme-dark-on-light` は開き直した後にもう一度）を computed 値で確かめ、開いたままのペインを `<番号>-<ケース>-inline-pane.png` に撮ってから Escape で閉じる）、左下に表示するレイアウト（`visible-layouts`: 通常マップ・階層図だけにすると左下がその 2 つになり（`hidden` と `display: none`）、ノード数と原文は不変。`visible-layouts-current`: 同じノートをタイムラインで開く（view state）とそのノートではタイムラインのボタンが出て選択状態、左右バランスは隠れたまま。`visible-layouts-switch`: 「通常マップ」でタイムラインのボタンが消えて 2 つに戻り、開き直しても 2 つ、`mappy-layout` は書かれない。`visible-layouts-back`: 4 つに戻して `hidden` がなくなる）、`free-topics` でのフリートピックの追加（空白のダブルクリック→入力→Enter）・Undo／Redo・ドラッグ移動（位置ありと位置なし、Escape での取り消し）・削除→Undo→Redo・ノードへのドラッグで合流（スロットの事前表示、区画→枝、Undo）・マウスでトピック・本体を運びながらタッチで左下の「左右バランス」を押す（`topic-drag-layout-button-touch`・`body-drag-layout-button-touch`: 切替で木も scale も動かず、その後の 30 px の移動に付いてきて、離したあとのビューポートが「全体表示」と一致する。LEV-182。原文は戻して通常マップで開き直す）・トピックをノードの隣に運ぶだけでスロットが出ること（`topic-snap`。ポインターは相手に乗らない。階層図では葉の真下（`topic-snap-hierarchy`）、タイムラインでは子のないステージの真上の森が始まる位置（`topic-snap-timeline`）と軸上のステージの隙間（`topic-snap-timeline-axis`）、「回復する」に画像を足して高さの違うステージにしたうえで「習慣化する」の最初の子が置かれる位置（帯の 34 単位上。ステージ自身の上辺からは 72 単位より離れる）に運んだときのスロット（`topic-snap-timeline-band`。fixture は原文に戻す）、左右バランスでは左側の葉「ふりかえる」の左隣（`topic-snap-balanced`）で同じことを確認する。「参考資料」の項目を消して見出しだけのトピックにし、「位置のないトピック」を右の空白（着地点と同じ高さ、ルートの右 120 単位以上）へ運んでから左へ水平に「参考資料」の最初の子が置かれる位置（右辺の 80 単位先。枝の zone の 72 単位より離れる）に運んだときのスロットと、離したあとの子が親の右辺の 80 単位先・同じ高さに置かれることを通常マップ（`topic-snap-root-gap`）と左右バランス（`topic-snap-root-gap-balanced`）で、階層図では下辺の 48 単位下・中心を揃えた位置（`topic-snap-root-gap-hierarchy`）で確認する（LEV-90。fixture は原文に戻す。階層図のケースだけは「補足: 用語」に `hierarchy` の位置を与えて列から外した変種で走る: LEV-125 でドラッグ中ずっと列を留めるようにしたため、列のままだと着地点に重なってポインターがその上に落ちる）。3 ケースとも「参考資料」の矩形をドラッグ前・右の空白・スロット表示中に読み、1.5 px 以上動かないことを検査する（LEV-95。左右バランス・階層図では「参考資料」は位置未設定で本体の下の列にあり、修正前は仮ノードで広がった木が列の中央揃えと運んだ木を避ける積み直しで跳んだ）。子を持つ位置未設定トピックの子列のケース（LEV-117・LEV-125）は、jsdom で 4 レイアウトを同じ原文から検査し、親をドラッグ開始前・`snapTarget` 決定時・`previewDrop` 後で比較する（末尾のスロットは、子が列に並ぶ通常マップ・左右バランスでは最後の子の下半分、子が横に並ぶ階層図・タイムラインではその右半分）。headless Chrome の `topic-snap-unplaced-child-column-balanced`／`-mindmap`／`-timeline`／`-hierarchy` は `free-topics` の「位置のないトピック」を「補足: 用語」の子列・子の行（左右バランスは右列の「用語 A」の後ろ、通常マップは「補足: 用語」の `mindmap` の位置を外した変種で「用語 B」の後ろ、タイムラインは軸の「用語 B」の右半分、階層図は子の行の「用語 B」の右半分。いずれも原文 index 2）へ実ポインターで運び、運ぶ 12 段階の各段階と仮ノード表示中の親の移動が 1.5 px 未満であること、仮ノード・青線・`is-merging`、ポインター自体は子の外（スロットはルートの位置で決まる）、合流後の原文、frontmatter のバイト一致、Undo／Redo のバイト一致を記録する。レイアウトは view state で切り替えて Fit し、`mappy-layout` は書かない）・本体のドラッグ（viewport の追従と全項目の書き換え、Undo）・本体の枝を空白へドラッグして切り離し（枝→区画、離した位置、Undo）・空白の右クリックメニューを実行し、原文と `mappy-topics` の前後を比較する。未選択の呼び出し（`call-map-topic`、E35 の「未選択→トピック」のこのページ版）は `free-topics` を fixture の原文に戻して開き、「回復する」をクリック → 空白をクリック（`is-selected` のノードがなくなり、フォーカスはキャンバス）→ `h.callMap("Fixtures/embed-timeline.md")`（検索モーダルで選んだのと同じ経路。モーダル自体はこのページにない）で文末に `## ![[embed-timeline]]` の 1 区画だけが足され、frontmatter と本体が不変、位置が書かれず、「講座の進行」が `is-topic` かつ `is-called-root` のトピックのルートとして現れて子 4 つが `aria-readonly` の枝、重なりなし、新しいトピックが選択されインライン入力は開かないこと、右クリック「元に戻す」で区画ごと消え `embed-timeline` が不変なことを確認する。SVG／PNG 書き出し（M13）は `uneven-branches` で「多数の兄弟」を閉じてから `window.__mappyHarness.export.svg()`／`png()` を呼び、SVG（`export-uneven-branches.svg`）の `foreignObject` 数・線の数・バッジ 24・画像がすべて data URL であること・欠落画像のノードが残ることを DOMParser で確かめ、その SVG を Chrome の別タブで開いて各 `foreignObject` が宣言した座標に描かれ画像が復号されることを読み取り撮影する（`export-uneven-branches-rendered.png`）。PNG（`export-uneven-branches.png`）は IHDR の寸法が SVG のサイズ × 2 に一致すること、`performance-2000-links` では SVG と PNG が完了し PNG がデスクトップのピクセル上限に収まることを記録する。埋め込み（`embed-*`、E34 のこのページ版）は `embed-host` を閲覧モード相当で読み込んで 5 つのマップと 3 つの通常の埋め込みを確認し（各マップのレイアウトが元ノートの `mappy-layout` に一致、ノードが枠内、scale ≤ 1、ホストと元ノートの原文不変）、階層図の埋め込みのルート直下の件数（4／2／1）と部分木のルート、開閉ボタンのクリック（一段だけ開いて枠に収まり直し、再クリックで戻る）、2,000 ノードの埋め込み（ルート＋13 ノードだけを描き、ホスト全体が 1 秒以内に安定）、元ノート `embed-timeline` の書き換えと復元での再描画、「閉じて開き直す」（live の数が増えない）、ページの暗色テーマ、`embed-host-live`（容器 5 つ、Obsidian の内容は非表示、通常の埋め込みは中身が見える）、`embed-host-live-late`（`embed-live-late`: 容器の接続が 3 フレーム、画面の下の 2 つは 90 フレーム遅れ、1 回目の描画が捨てられても 5 つとも claim されてマップになり、容器 5・live 5・枠 5。区画を渡した直後の待ち（`HostTiming.waiting`）は 0 より大きく、800 ms 後は捨てられた描画の区画（`HostTiming.discarded`）だけが待ち、その間の製品のフレームは 100 未満（polling なし）、区画に残る anchor はマップの数と同じで、live は増えない。`embed-live-dispose` で待ちも 0）、解放（`disposeEmbeds()`: 枠と Component が消え、ライブプレビュー相当では容器の内容が再び見え、閲覧モード相当では placeholder が元の src で戻る）を確認する。マップの中の呼び出し（`embed-node-*`、E35 のこのページ版）は `embed-nodes` を開いて 5 つの項目がそれぞれ呼び出し先のルートの文（タイムラインの「講座の進行」・`#同じ名前` の部分木・2,000 ノードの「講座（2000ノード）」・同じマップの 2 回目・循環の相手）になり、ルートの子（4／2／13／4／3）が枝として並び、それより下は件数バッジで折りたたみ、呼び出したノードは `is-called` で `aria-readonly`・読み上げの説明に元ノートのパス・ホストと違う文字色、項目に `link` の印、枠なし、編集要素なし、リンク 5・画像 1、ノードの重なりなし、どのノートも変わらないことを確認し、タイムライン・階層図・左右バランスで開いて呼び出した木も現在のレイアウトの class で重なりなく配置されること（左右バランスでは側に従う）、呼び出したノード「第 2 週: 回復」のクリック（そのノードが選択）と開閉ボタン（一段開いて 2 ノード増え、もう一つの同じマップは閉じたまま）と Space（閉じる）、そのノードで Enter／Tab／Delete／F2（原文不変・インライン入力なし・Notice「読み取り専用」）とドラッグ（ゴースト・仮ノードなし・原文不変）と右クリック（「元のマップを開く」「折りたたみ」「元に戻す」「やり直す」だけ）、呼び出したノードのダブルクリック（元ノートを開く要求、インライン入力なし）、項目「講座の進行」の F2（原文 `![[embed-timeline]]`）→ Escape → Delete（項目ごと 5 ノードが消え、もう一つの同じマップは残る）→ 右クリック「元に戻す」（戻る）、元ノート `embed-timeline` の書き換えと復元（2 つの枝が描き直され、ホスト不変）、存在しなかった「存在しないノート」を `mappy: true` のノートとして作って消す（ホストを編集しなくてもリンクが枝になり、消すとリンクに戻る）、2,000 ノードの「ノード 1」の開閉（13 ノード増えて戻る。開くのにかかった時間）、「全体表示」→ SVG 書き出しとシーン（`foreignObject` とブロックがノード数と一致し、呼び出したノードの class が入る。`export-embed-nodes.svg`）、`embed-cycle`（項目「呼び出しの検証」が枝になり、その中の「循環（…）」を開くと `![[embed-cycle]]` はリンクのまま呼び出されない。自分自身もリンク）、「閉じて開き直す」（同じ表示、購読数が初回と同じ）を撮影する。ホバーの吹き出し（LEV-199）は Obsidian の機能でこのページには出ないので、Obsidian 1.14.2 の app.js と同じ判定（乗せた要素から最も近い `[aria-label]` の祖先を探し、その要素の算出値 `--no-tooltip` が `true` でなければ、その要素の下に吹き出しを出す）を実 CSS の上で再現して数える: `node-tooltip`（`embed-nodes` の自分のノード・呼び出したノード・入力中の欄のどの要素でも 0、開閉ボタンと左下・右下・右上のボタンは自分の `aria-label`）と `embed-node-tooltip`（閲覧モードの埋め込みのノードで 0、開閉ボタンと「マップで開く」は自分の名前）。吹き出しが実際に消えたことの確認は実機でしかできない。各ケースの PASS／FAIL、時刻、全画面とペイン 2 倍のスクリーンショットを `artifacts/browser-harness/<日時>/record.md` に書く。Chrome がなければ未実施と書いた record だけを残して終了コード 2 になる。headless Chrome 153 は修飾キー付きのキー入力（⌘Z・⌘⇧Z）を CDP で繰り返すとブラウザプロセスが応答しなくなることがあるため、フリートピックのケースの Undo／Redo は右クリックメニューで行い、プロトコル呼び出しが 60 秒応答しなければ以降を FAIL として record を書いて終了する（`MAPPY_CAPTURE_TRACE=1` で呼び出しを逐次表示）。
- 対象外: 保存（Vault・Editor への書き込み。SVG／PNG 書き出しもファイルにせず文字列を返すだけで、添付設定の保存先は③）、リンク解決（クリックは通知と記録だけ）、Obsidian の配色（`harness.css` の CSS 変数は仮の値。ただし app.css と同じ構造——素の配色を `.theme-light`／`.theme-dark`、意味変数を `body` に置く——にしてあり、設定のテーマが class と変数の導き直しで切り替わることまでは確認できる。実際の配色とコミュニティテーマは③。書き出しの色もその仮の値。`button` には app.css 1.14.2 の規則——`button` の箱（`inline-flex`・中身の中央寄せ・高さ 30px・余白）、`button:not(.clickable-icon)` の面と影（明暗別の `--input-shadow`）、`(hover: hover)` の中の hover、`aria-disabled` の opacity——を同じセレクタで写してあり、製品のボタンが実機と同じ詳細度の規則と競合する（LEV-84 のポップオーバーの行のずれはこれで再現した。`.mod-macos`・`.is-tablet`・`.is-mobile` の規則は写していない: LEV-85））、Vault の画像の読み取り（このページの画像は data URL なので resolver は素通し）、日本語 IME（合成イベントは OS の変換ではない）、トラックパッドとモバイルの実入力。これらは③で確認する。

このページでの成功を Obsidian の保存・リンク解決・テーマでの成功と呼ばない。Obsidian 実機の確認（下記 E01〜E38）が E2E に相当し、このページはその代替ではない。

### Obsidian 実機の初回準備

1. まだ試用していない専用環境で `npm run harness:prepare` を実行する。生成するのはこのプロジェクト内の `test-vault/` のみ。
2. Obsidian でそのフォルダを Vault として開く。必要な初回の制限モード設定はテスト環境で行う。M6 のケース（E23〜E27・E30・E33）には Excalidraw（`obsidian-excalidraw-plugin`）をこの Vault にインストールして有効にする。
3. `npm run harness:preflight` を実行する。これはファイルと設定の検査であり、実行中プラグインが最新である証明ではない。有効プラグインは mappy と Excalidraw（M6 用）だけを有効にする。それ以外が有効なら preflight は失敗し、実機確認の条件に含めない。
4. プラグインを再読込し、対象 Vault と機能の挙動を画面で確認する。将来は表示する build ID も照合する。
5. 下記ケースを再現し、UI の状態と変更後の Markdown を両方保存する。

`harness:prepare` は既知の fixture を初期化するため、ユーザーが試用中の Vault には再実行しない。生成 fixture に書いたノードも利用者の変更として保持する。再実行した場合、`community-plugins.json` は mappy と、すでに有効なら Excalidraw だけを残して書き直す（Excalidraw の配布物と設定には触れない）。既存の Evergreens Vault や taskchute-plus の配布物は操作しない。

### 試用中の更新

製品コードを変更したら、配布物だけを更新する。作業ディレクトリがこのプロジェクトであることと、実機の Vault がこのプロジェクトの `test-vault/` であることを確認する。

```sh
npm run check
cp dist/mappy/main.js dist/mappy/manifest.json dist/mappy/styles.css test-vault/.obsidian/plugins/mappy/
npm run harness:preflight
```

その後、専用 Obsidian 環境で Mappy だけを再読込して対象画面を開き直す。`preflight` の成功だけでは実行中コードの更新は確認できないので、新しい表示・操作も確認する。Markdown、添付ファイル、workspace 設定や他のプラグインはコピーしない。再現用ノートが必要なら専用 Vault 内に別名で作り、既存ノートを上書きしない。

画面変更の証跡には、通常マップ／タイムラインのスクリーンショット、折りたたみと件数、インライン入力・リンク候補、操作前後の Markdown を含める。試用中のユーザー入力がある場合は、検証時の変更と区別して記録する。

| ID | 実機ケース | 期待する結果 |
| --- | --- | --- |
| E01 | 分割で日本語見出しを入力 | 変換確定で余計なノードが増えずマップへ反映 |
| E02 | map で Enter/Tab/F2/Delete（LEV-155。`npm run harness:e2e:add-delete`） | フォーカス範囲内だけに作用し本文・子孫を保持。Delete／Backspace／「枝を削除」はリストの項目の行を改行ごと消し、リストの中に空行を残さない（次の兄弟との継ぎ目の空行は一緒に消え、loose list の継ぎ目の空行は 1 つ、末尾の項目は前の改行を取ってファイル末尾の改行の有無を変えない。ブラウザ検証ページで確認済み: `artifacts/lev-75-delete-newline/record.md`。実機: 2026-09-22、`scripts/e2e/add-delete.mjs`。既存の項目の間で Enter（兄弟追加）→ タイトル確定 → Tab（子追加）→ タイトル確定 → Delete（子）→ Delete（兄弟、リストの中間の項目）の往復が原文とバイト一致し、`\n\n\n` のような空行が残らないことを固定する。`artifacts/lev-155-e2e-cases/`）。⌥↑／⌥↓ でリスト形式の区画（H2）または見出し形式の区画を入れ替えると、区画の間の空行の数とファイル末尾の改行の有無が変わらず、往復で原文に戻る（例外: 寄せ合わせた行が Setext 下線や HTML ブロックに融合する配置では継ぎ目に空行を 1 つ足す。深さの飛んだ兄弟は隣の深さに揃うので深さは戻らない。LEV-87・LEV-88。core の vitest で確認: `artifacts/lev-87-section-blank-line/record.md`、`artifacts/lev-88-heading-swap-eol/record.md`。区画（H2）の入れ替えの実機は未実施——E19（LEV-155）はリスト項目の入れ替えで別の観点を固定しており、この区画入れ替えの代わりにはならない）。LEV-140・LEV-142・LEV-146 を revert しても本ケースは PASS のまま（その再発は捕まえない。手順と結果は下記「revert 検証（LEV-155）」） |
| E03 | 編集→Undo→Redo→表裏切替（LEV-155。`npm run harness:e2e:undo-redo`） | 文書とマップが同じ内容、履歴の二重適用なし（実機: 2026-09-22、`scripts/e2e/undo-redo.mjs`。F2 改名 1 件（`DocumentStore.apply` 1 回）を ⌘Z→⌘⇧Z、続けて `mappy:toggle-mindmap` で Markdown⇄map を往復してから ⌘Z→⌘⇧Z→⌘⇧Z（末尾で no-op）し、各段階で `app.vault.read` がバイト一致し履歴が消えも二重適用もされないこと、表裏切替の前後でディスクと Markdown editor の内容が一致することを固定する。Enter/Tab の追加は「空ノード作成」「タイトル確定」が別々の `DocumentStore` エントリになる（実機で確認済み）ため、1 手順＝1 Undo に保つ本ケースの編集は F2 改名を使う。LEV-140・LEV-142・LEV-146 を revert しても本ケースは PASS のまま（その再発は捕まえない。手順と結果は下記「revert 検証（LEV-155）」）） |
| E04 | 同名ノードの片方を移動 | 正しい本文・子孫だけが移動 |
| E05 | 外部更新と map 編集を競合させる | 古い状態で上書きせず入力も復旧可能。保持した入力はマップの自動更新後にもう一度 Enter すれば更新後の文書に適用される。編集中のノードのタイトル／本文が外部で変わっていた場合、同名ノード、消えたノードでは拒否されて入力が残る（同じタイトル・本文のまま位置だけ変わったノードには適用する）。拒否のあと view 自身が再読込し、inline と本文モーダルのエラー行が「Markdown が更新されました…」に変わる。拒否するのは外部の変更に対してだけで、マップ自身の書き込み（編集中のノードへの画像の追加、下書きを開いたままのコマンド）では拒否もエラー行も出ない（LEV-140。編集中に画像を貼り、そのまま Enter で確定できることを見る） |
| E06 | 内部/外部/別名/見出し/ブロックリンク | 元ファイル基準の正しい遷移。map は navigation view（LEV-74）: クリックは map の leaf を置き換え（Markdown タブと同じ）、⌘クリックは新しいタブ、ピン留めした map は置き換わらず隣の navigable なタブか新しいタブに開く、戻る（⌘⌥←／ヘッダーの ←）で map に戻る（選択ノードも戻る）。`[[#見出し]]`（同じノート）・`[[ノート#見出し]]`・`[[ノート#^ブロック]]` はその位置のノードが選択されて見える。inline 入力中にリンク・エクスプローラー・⌘⌥← で別のノートが入ると下書きが保存されてから移る。inline 入力のないノードで Escape を押しても active leaf とフォーカスは map に残り、F2・パスのコピー・削除などコアのファイル系コマンドは map で「対象なし」（隣のノートを対象にしない）。内部リンク・見出し・ブロック・⌘クリック・ピン留め・戻る・別ウィンドウは実機で確認済み（`artifacts/lev-74-view-navigation/record.md`） |
| E07 | 画像追加、遅延、欠落、サイズ指定 | 添付設定に従う、重なりと表示位置の飛びを抑える |
| E08 | ピンチ/ホイール/パン/Fit | 意図した中心でズーム、通常エディタへ干渉しない |
| E09 | 開閉50回・再読込・別ウィンドウ | 多重イベント、残留DOM、継続的なメモリ増加なし（開閉・再読込は E51、別ウィンドウは E50 が自動で固定する。LEV-14） |
| E10 | 500/2000ノードとタイムライン | 性能指標を記録、内容欠落なし（長文・画像・深い枝の混在文書のタイムラインは E45） |
| E11 | モバイルと明暗テーマ | タッチ、フォーカス、文字・リンク・画像を操作可能。右上の歯車のポップオーバーがタッチで開いて 3 項目を選べ、明暗テーマで読める（デスクトップの明暗テーマは E48 が自動で固定する。モバイル・タッチは未実施。LEV-14） |
| E12 | H2＋リストと、従来の H2→H3→H4 をそれぞれ表示 | 各形式の実際の深さに従う。ルートは濃い面、第一階層は枠付き、下位は平文で下線なし |
| E13 | 分岐点にホバー→多段の枝を閉じる→再展開 | 分岐点で丸い − を表示。閉じると全非表示子孫の件数を示し、3・4桁の数字も別の線やノードと重ならない |
| E14 | ノードの追加→テキスト入力 | 追加モーダルがなく、その場で入力できる。LEV-203（本人の決定 2026-09-26）から、Enter／Tab の子・兄弟は「サブトピック」、空白のダブルクリック・右クリックのフリートピックは「トピック」という仮の名前で書かれ、その名前が全選択された入力欄が開く（打てば上書き。入力欄の `placeholder` 属性は使わない）。詳細は E43 |
| E15 | `[[` / `![[`→ノート・別名・PNG・SVG・PDF 候補→Enter/Tab | 候補選択で余計な保存・子追加をせず、周囲の入力・拡張子・埋め込み記号とリンク先を保持 |
| E16 | 全面キャンバス・浮かせた UI・右クリックの操作 | 操作専用の帯を割かず、左下はレイアウト、右上は歯車 1 つのポップオーバー（Markdown に切り替え／マップを検索して呼び出す／書き出す の 3 項目。歯車の直下に右辺を揃えたカードで開き、幅 400px のペインでも左辺がペインの左端を越えない。開いた直後は 1 項目目にフォーカス、↑↓で移動、Enter／Space で実行、Escape・外側のクリック・もう一度の押下・view を閉じる で閉じてキャンバスにフォーカスが戻り、フォーカスがカードの外へ移ったとき・ウィンドウが非アクティブになったときはフォーカスに触れず閉じる。低いペインではカードの中がスクロール。ノートなしでは全項目無効。各項目はコマンドと同じ経路。右クリックメニューは変えない。実機: 2026-09-20、`artifacts/lev-80-popover-e2e/record.md`。歯車 1 つ・右揃え・3 項目とアイコンの左端・マップとアプリの明暗・↓／Escape／外側の押下・3 項目の起動・ノートなしの無効・popout は確認済み。未実施: モバイルのタッチ（E11）、幅 400px 未満のペイン（実機は popout 560px まで。headless Chrome の `action-popover-narrow` で確認済み））、右下はズーム |
| E17 | Markdown のリストをインデント・アウトデント（LEV-17。`npm run harness:e2e:deep-branches`） | 階層へ即時反映し、6段階を超える枝も追加・編集できる（実機: 2026-09-25、Obsidian 1.14.2、`scripts/e2e/deep-branches.mjs`。8 段のリストで、最深の項目への Tab（9 段目）と Enter（兄弟）が隣と同じ 2 スペースの段で書かれること、既存の 9 段目の F2 改名とその戻し、子を持つ 8 段目の ⌥↓／⌥↑ の往復、7 段目の部分木を 2 段目へ・2 段目の項目を 8 段目へ実ポインターでドラッグして部分木の全行が再インデントされることとその ⌘Z を、各段で原文のバイト比較で固定する。横に開いた Markdown エディタで実キーの Tab／Shift+Tab を送り、Obsidian の保存（約 2 秒）より前の 1 秒以内にマップの親が変わること（`editor-change` を追う「即時反映」）と、保存後の各リスト行でマップの深さがエディタのライブプレビューの list level（`HyperMD-list-line-N`）と一致すること（エディタがリスト項目として描く行にマップのノードが無い場合も不一致）を確かめる。前提の `useTab: true`・`tabSize: 4` でなければノートを書く前に止め、エディタは編集モード（ライブプレビュー）に切り替えてから確かめる。1 秒以内の条件はマップの解析だけでなく、そのノードの描画位置が動いたことも含む。ドラッグが原文を変えなかったら ⌘Z を送らずに止める。Obsidian の Tab は Vault の設定（既定の `useTab`・`tabSize` 4）で 4 桁ずつ動かしタブを混ぜるので、落ち着く親は CommonMark の桁で決まり、推測ではなくエディタを基準にする。閲覧モード・metadataCache はタブの混ざった行を別の階層に読むので記録（`reading`）だけにする（LEV-195）。同じノートを開いた leaf が既にあると map の編集が Editor 経由になり保存が遅れて原文の読み取りが古くなるので、最初に止める。`artifacts/lev-17-deep-branches/`） |
| E18 | 旧見出し形式→「リスト形式に変更」→Undo（LEV-17。`npm run harness:e2e:convert-to-list`） | 自動変換せず、明示操作で同じタイトル・構造を保って変換・復元（実機: 2026-09-25、Obsidian 1.14.2、`scripts/e2e/convert-to-list.mjs`。H1 のルート・H2〜H6・本文・同名の見出し 2 つを持つノートを開いても原文が変わらず見出し形式として読まれること、見出し形式の H6 への Tab が「見出しは 6 階層までです。」で拒否され原文不変（M2「H6 を超える見出しを生成せず」）、コマンド `mappy:convert-to-list` の結果が期待した H2＋リストの原文とバイト一致しマップの（タイトル ← 親）の並びが変換前と同じこと、⌘Z で原文にバイト一致・同じ構造、⌘⇧Z で同じ変換、変換後は同じ Tab で 7 段目が入ることを固定する。`artifacts/lev-17-deep-branches/`） |
| E19 | 子リストの前後に親の本文がある項目を編集・移動（LEV-155。`npm run harness:e2e:move-parent-text`） | 子を動かしても親の後続文章を持ち去らず、本文・画像追加で既存の枝が変わらない（実機: 2026-09-22、`scripts/e2e/move-parent-text.mjs`。子リストの後に空行を挟んだ項目（空行がないと CommonMark の lazy continuation で末尾の文が最後の子の本文に飲み込まれ、親の本文でなくなる）で子 2 つを ⌥↓・⌥↑ し、親の後続文章が消えず重複せず、往復で原文がバイト一致すること、最初／最後の子を超える ⌥↑／⌥↓（`src/core/list-commands.ts` の `move()` が隣接する子の `from`／`to` だけを入れ替えるので兄弟がなければ no-op）が親の兄弟（`記録する`）へ越境しないこと、選択中（編集中でない）の親へ画像を貼ると子リストより前（本文側）に入り子リスト・後続文章が動かないことを固定する。`artifacts/lev-155-e2e-cases/` |
| E20 | 「マップと Markdown を切り替え」を同じ leaf で往復 | 未保存内容と選択位置を保ち、Markdown 側は選択ノードの行にカーソル。切り替えのあと ←（⌘⌥←）で前の面に戻り → で進める（map は navigation view。LEV-74）。← で Markdown に戻った leaf は E22 と同じくその後も Markdown（実機で確認済み: `artifacts/lev-74-view-navigation/record.md` の 09〜09g） |
| E21 | 通常ノート→「このノートをマインドマップ化」→解除、新規マップ作成 | 通常ノートは変換前にマップで開けない。変換で `mappy: true`、解除で Mappy のキーだけが消える。新規マップは H2 ルートを持つ |
| E22 | トグルで Markdown にした leaf で同じノートを開き直す | Markdown のまま。別 leaf や別ノートには影響しない |
| E23 | Excalidraw で Option を押して `.md` をドロップ／修飾なし／Shift | Option だけがマップ要素を挿入し、他は Excalidraw 既定（リンク・画像）のまま |
| E24 | Excalidraw の対話フレームに `mappy: true` のノートを挿入（LEV-22。`npm run harness:e2e:excalidraw-frame`） | Mappy のビューがライブ表示され、線・枠・リンクが見える。通常ノートは Markdown のまま（実機: 2026-09-25、Obsidian 1.14.2・Excalidraw 2.27.3、`scripts/e2e/excalidraw-frame.mjs`。`addEmbeddable` で置いた 2 枚の枠の DOM を `#embed-<要素 id>` で要素ごとに読み、マップの枠が `mappy-map` の leaf で、ルートと第一階層が枠付き・線が長さのある SVG path・リンクが `a.internal-link`・全ノードが枠の中に Fit、通常ノートの枠は Markdown の見出しとリンクで map の DOM が無いことを固定する。`--shot` で 2 枚を並べて撮る。枠を置く前に、Mappy を一時的に外した状態で map ノートが Markdown で開くことを確かめる（前に試した壊れたビルドが prototype に残した routing があれば、ルーターの無いビルドでも枠がマップになって PASS してしまうため、そこで止める）。Excalidraw のダイアログは通さない。`artifacts/lev-22-excalidraw-lifecycle/`） |
| E25 | Mappy 無効化→Excalidraw 再読込→Mappy 有効化（LEV-22。`npm run harness:e2e:excalidraw-lifecycle`） | 無効時はフックと差し替えが外れ、有効化後にドロップが再び効く（実機: 2026-09-25、`scripts/e2e/excalidraw-lifecycle.mjs`。Excalidraw 再読込（Mappy 有効）→ Mappy 無効化 → Excalidraw 再読込 → Mappy 有効化 → Mappy 無効化・再有効化の各段で、`mappy: true` のノートを `setViewState({ type: 'markdown' })` で開いた view 型、`WorkspaceLeaf.prototype.setViewState` と `onDropHook` の前の段との同一性、ファイルエクスプローラーから図面への Option ドロップ（CDP の合成ドラッグ。Excalidraw 自身の drop 処理を通る）の結果を見る。最初に Excalidraw を再読込するのは、起動順に関係なく Mappy の無効化を「Excalidraw の wrapper の下」（素通しになり外側は変わらない）と「上」（Excalidraw の残した関数に戻る）の両方で踏むため。プラグインの切替は `community-plugins.json` を書き換えない API で行い、最後に両方を有効に戻す。`artifacts/lev-22-excalidraw-lifecycle/`） |
| E26 | タイムライン／階層図を選択→Markdown へ切替→マップへ戻る | `mappy-layout: timeline`／`hierarchy` が保存され、再度そのレイアウトで開く。通常マップ選択では任意キーだけを削除する |
| E27 | Mappy ノートを Excalidraw の「Insert as embeddable」「Insert image」で挿入 | 「Insert image」はマップを描いた SVG 画像（M13 と同じ見た目、縦横比はマップ、長辺 500、`link` は元ノート。図面の添付先に `<ノート名>.svg` ができる）、「Insert as embeddable」は枠がマップの縦横比（長辺 800 まで）で左上は置いた場所のまま。新しく作られた Mappy 埋め込みの外枠が透明。通常ノート・既存要素・内容・リンク・元ノートは変えない。図面を開き直しても画像がマップのまま（Markdown の文字に戻らない）。LEV-118 の実機は 2026-09-21 に、Mappy の見張りによる置き換えだけを確認済み——ダイアログのボタンは押さず、Mappy の `onDropHook` に修飾なしの file drop を渡してから、ダイアログが作る要素を EA の `addImage`／`addEmbeddable` で代わりに足した（Obsidian 1.14.2、Excalidraw 2.27.3、PR #62 のブランチ `d8bf1b3` のビルド。`artifacts/lev-118-excalidraw-e2e/record.md`。Insert image は 442 ms で 394×500・縦横比 0.788・`link: [[uneven-branches]]` の SVG 画像に置き換わり添付 `uneven-branches.svg`（47 ノード）ができ、開き直しても Markdown に戻らない。Insert as embeddable は 400×500 の枠が 610 ms で 637×800・縦横比 0.796・`strokeColor: transparent` になり左上は不動。`mappy: true` のない通常ノートは Excalidraw 既定のまま。元ノートは不変）。根拠は `artifacts/` の probe（`run.mjs`）で `scripts/e2e/` には未登録。未実施: ダイアログのボタンを押す本来の経路、複数ファイルの同時ドロップ、マップでなくなったノートの Notice、実ポインターでのドラッグ |
| E28 | Enter／Tab でノードを追加し（LEV-203 から仮の名前「サブトピック」が全選択で開く）、長い文字列を入力 | 入力欄は開いた直後は仮の名前の幅で、打ち始めると文字の幅になり、1 行のまま文字に合わせて横へ広がり、確定後のノードと同じ幅（ノードの上限 420px／ルート 360px から余白を引いた幅）を超えると 2 行目へ折り返す。確定後のラベルも同じ位置で折り返す（入力中 1 行なら確定後も 1 行。記法・連続する空白・URL（外部リンクのアイコン）を含む文は除く）。確定後のノードの見た目は変わらない。左右バランスの左側の枝は親側の辺を保って左へ伸びる。入力・補完・保存は従来どおり動く（LEV-198） |
| E29 | 空白をダブルクリックしてフリートピックを追加→本体のノードへドラッグして合流→Undo→Mappy 以外（通常の Markdown 編集・閲覧モード、Mappy 無効）で開く。同名トピック（LEV-86）: Markdown 側で `## 同じ見出し` の区画を 2 つ書く→2 つ目をドラッグ→1 つ目を改名→Undo→1 つ目を削除→Undo | 文書末尾に最上位区画が追加され、合流で区画がリストの枝に変わり、Undo で元の区画と位置に戻る。本体のノード・本文・他の frontmatter は変わらず、Mappy 以外では通常の見出しと箇条書きとして読める。同名トピックは 2 つ目だけが動いて `同じ見出し (2)` のキーが書かれ、1 つ目は動かず選択も動かした方のまま。改名で `(2)` のキーが `同じ見出し` に繰り上がり、削除でも残る方の位置が変わらない（実機: 2026-09-20、`artifacts/lev-41-topics-e2e/record.md`。追加・レイアウト別の位置保持（`mindmap`／`timeline` のキーが隣に並ぶ）・合流と Undo・Mappy 以外（ソース・閲覧モード・無効化）で開く・同名トピックは確認済み。子を持つ位置未設定のトピックの子列への合流（LEV-117・LEV-125）は 2026-09-21 に Obsidian 1.14.2 の test-vault で確認: `free-topics` の複製を 4 レイアウトの view state で開き、実ポインターで「位置のないトピック」を「補足: 用語」の子列（階層図・タイムラインは子の行の末尾）へ運ぶ間（12 段階と仮ノード表示中）の親の移動 0.0 px、離すと 用語 B の後ろへ合流、⌘Z／⌘⇧Z で原文と合流後をバイト一致で往復、複製は削除し `free-topics.md` は不変（通常マップ・左右バランスは `artifacts/lev-117-topic-parent-stability/record.md`、階層図・タイムラインは `artifacts/lev-125-topic-parent-layouts/record.md`。どちらも `obsidian-run.mjs`）。ただしこの 4 回はいずれも本人の試用設定のまま（dataview が有効）の vault で走らせており、上の「mappy と Excalidraw だけ」という実機確認の条件は満たしていない。条件を満たす vault での再実行は未実施。未実施: タッチ・トラックパッドの実操作感、Properties（block 形式）でのキーの目視） |
| E30 | 階層図のマップをアクティブにして「現在のマップを Excalidraw の図面に挿入」 | 挿入した要素が 1 グループで、ルートが上・第一階層が同じ段・線が直角の折れ線。E27 の外枠透明化とは別のケース |
| E31 | 設定タブで 4 項目を変更 → 既存ノートを開き直す → 新規マップを作成 → Obsidian の外観を明暗で切り替える → Obsidian を再起動。左下に表示するレイアウト（LEV-76）: マップを開いたまま設定タブでタイムラインをオフ → `mappy-layout: timeline` のノートを開く → 左下の「通常マップ」→ 既定レイアウトをタイムラインにして注記を見る → 新規マップを作成 | 設定タブに 4 項目だけが並ぶ（1.13 以降は設定検索にも出る。左下に表示するレイアウトは 1 行にトグル 4 つで、通常マップは on 固定で操作できない）。既存ノートの本文・frontmatter・表示レイアウトは変わらない。新規マップは作成先フォルダに `mappy-layout` 付きで作られ、そのレイアウトで開く。テーマの明示指定では map view のコンテナだけが明色／暗色になり、リンク・コード・画像・浮かせた UI・インライン入力が両方で読める。「Obsidian に従う」で外観に追従し、再起動後も設定が残る。コミュニティテーマでも破綻しない。タイムラインをオフにすると開いている map view の左下からボタンが即時に消え（ノートは不変）、タイムラインのノートではそのボタンが出て選択状態、「通常マップ」で消えて `mappy-layout` が削除される（従来どおり）。既定レイアウトが非表示だと設定タブに注記が 1 行出て、新規マップはその既定で作られてそのノートではボタンが出る。再起動後も非表示のまま。モバイルでトグルの行が折り返して操作できる（実機: 2026-09-20、`artifacts/lev-62-settings-e2e/record.md`。4 項目の表示・逆テーマの配色・「Obsidian に従う」の追従・既定レイアウトと作成先（入れ子フォルダの自動作成）・マインドマップ化・左下のトグル（左右バランスの on／off で `visibleLayouts` が増減、通常マップは外れない）・`app:reload` 後の復元は確認済み。逆テーマでインライン入力のキャレットがアプリ側の色のままで見えなかった件は LEV-93 で直し、2026-09-20 に実機で computed style を確認済み（`artifacts/lev-93-caret-e2e/record.md`。逆テーマの両方向でキャレットの色がマップ側の文字色になる。目視ではなく値の読み取り。モバイルは未実施）。未実施: コミュニティテーマ、モバイル、設定の検索欄、右クリックメニューの見た目。非表示のレイアウトのノートでのボタンの表示と既定レイアウトが非表示のときの注記は record に記載なし） |
| E32 | 明色・暗色それぞれで、画像（Vault の PNG・欠落・外部 URL）と閉じた枝を含むマップを「現在のマップを SVG／PNG に書き出し」 | 添付ファイルの保存先に `<ノート名>.svg`／`.png` ができ、元ノートは不変。SVG を Obsidian 外のブラウザで開くと map view と同じ配置・折りたたみ・テーマの色で、Vault の画像が埋め込まれ、欠落画像のノードは代替テキストで残る。PNG は 2 倍の寸法。モバイルでは PNG の可否と縮尺（実機: 2026-09-20、`artifacts/lev-63-export-e2e/record.md`（record の見出しは E31 と表記）。SVG の明色・暗色・連番・保存先の追従・Obsidian の外での配置と折りたたみ・Vault の画像の data URL・欠落画像・PNG の 2 倍・2,000 ノードは確認済み。外部 URL の画像は fixture になく未確認。SVG の `data-theme` が body の `theme-dark` 依存で設定 `theme` と食い違う — LEV-92 で `themeOf` が canvas から最も近いテーマ class を読むよう修正（jsdom で固定、実機は未実施）。未実施: Excalidraw の対話フレーム内でのコマンドの表示、モバイル） |
| E33 | 左右バランスを選択→Markdown へ切替→マップへ戻る→左側の枝を閉じる→「現在のマップを Excalidraw の図面に挿入」（E26・E30 の左右バランス版） | `mappy-layout: balanced` が保存され、再度そのレイアウトで開き、通常マップ選択では任意キーだけを削除する。第一階層が原文順に右・左・右・左に分かれ、左側の枝の開閉ボタンと折りたたみバッジが左に出て、本文は変わらない。挿入した要素は 1 グループで、ルートが中央、第一階層が右・左に分かれ、左側の線がルートの左辺から子の右辺への直角の折れ線になり、第一階層の側・順序・直角の線がマップと一致する。ブロックの座標は Excalidraw 自身の文字幅で決まるのでマップの DOM とは一致しない（`excalidraw-bridge.ts` は原点に作って Excalidraw に測らせてから配置する設計。長い日本語のタイトルは DOM では折り返し、Excalidraw では 1 行になるのでそこから先が横にずれる。ブラウザ検証ページの `balanced-scene` は同じ測定器で測るので一致する）。実機で確認済み（2026-09-20、`artifacts/lev-61-balanced-e2e/record.md`: E26・E13・実ポインターの並べ替えと親変更・第一階層への差し込み・フリートピックの合流・E30／E33 の挿入・E10 の 2,000 ノードの左右バランス版。未実施: `scripts/measure-layout.mjs` への 2,000 ノードの追記、トラックパッドの実操作感） |
| E34 | `Fixtures/embed-host.md` を閲覧モード・ライブプレビューで開き、`[[embed-host]]` のホバープレビューも出す。元ノート `embed-timeline` を別 leaf で編集。埋め込み内の開閉ボタン・リンク・右上の「マップで開く」。ホストを閉じる。Mappy を無効化 | 3 つの表示すべてで 5 つの `![[…]]` が読み取り専用のマップ（通常・タイムライン・階層図・`#同じ名前` の部分木＝回復する の下の最初の一致・2,000 ノードはルート＋13）になり、`heading-document`・存在しないノート・`#^block` は通常の埋め込みのまま。明色・暗色テーマで枠・線・件数バッジが読める。別 leaf の編集（未保存でも）で埋め込みが更新され、ホストと元ノートの原文は変わらない。開閉はマップ内だけで原文を変えず、リンクは元ノート基準、「マップで開く」で元ノートがマップで開く。ホストを閉じたあと DOM・イベントが残らず（DevTools のメモリ／`app.workspace` のリスナー）、2,000 ノードの埋め込みがあってもホストの入力・スクロールが止まらない。無効化で通常の見出し＋箇条書きの埋め込みに戻る（実機: 2026-09-20、`artifacts/lev-64-embed-e2e/record.md`。閲覧モード・ホバープレビュー・明暗・別 leaf の編集（71 ms）・開閉・リンク・「マップで開く」・ホストを閉じたあとのリスナー・2,000 ノードのホストの rAF 間隔・無効化と再有効化は確認済み。この日のライブプレビューは 3 つ以上の埋め込みで一部が通常の埋め込みのまま残り不安定だった — LEV-91 で修正し（`claimContainer` が容器の接続を 1 フレームしか待たなかった）、2026-09-21 に実機で再確認済み（`artifacts/lev-91-live-preview-e2e/record.md`。接続を待つ版で `embed-host` を 5 回開き直して毎回 5 つともマップ、3 つ埋め込んだ一時ノートも 5 回とも 3 つマップ、開いて 8 秒放置してからスクロールしても画面の下の 3 つがマップになり、閉じたあとリスナーは基準値に戻る）。未実施: モバイル、PDF、同名見出しの大文字小文字） |
| E35 | 入力側（LEV-70）: `Fixtures/free-topics.md` をマップで開き、開いた直後（本体ルートが選ばれた状態）でコマンド「マップを検索して呼び出す」→ 候補を見る → `uneven` と打って絞り込む → Enter。「回復する」を選択して同じコマンドで `embed-timeline` を選ぶ。「回復する」の開閉ボタンで枝を閉じてから（足した `![[embed-timeline]]` の枠がトピックのルートを覆うため。LEV-79）「補足: 用語」（トピック）を選択して同じマップをもう 1 度、枝を開き直す。⌘Z を 3 回。設定「新しいリンクの形式」を相対／絶対にして再度呼び出す。未選択→トピック（LEV-83）: 「回復する」をクリックしてからキャンバスの空白をクリック（選択の枠が消える）→ 歯車／コマンド「マップを検索して呼び出す」→ `embed-timeline` → ⌘Z。同じ手順を見出しのないノート（`- 項目` だけの `mappy: true` のノート）でも。足したトピックのルートをドラッグして空白で離す → ノードの上で離す → ⌘Z ×2。「回復する」の子の `![[embed-timeline]]` を空白へドラッグ → ⌘Z。トピックのルートで F2 → Escape → Delete → ⌘Z。空白をクリックしてから ⌘Z（区画が戻る）。「記録する」を F2 → 文字を足す → 空白をクリック。表示側（LEV-82。枠だった LEV-69 を枝に置き換え。実機はこの形で取り直す）: 次のとおり。`Fixtures/embed-nodes.md` をマップで開く（`![[embed-timeline]]`・`![[embed-hierarchy#同じ名前]]`・`![[embed-2000]]`・同じ `![[embed-timeline]]` の 2 回目・`![[embed-cycle]]` だけのノードを持つ）→ 呼び出した枝のノードを選択して Enter／Tab／F2／Delete／Space／ドラッグ／右クリック → 呼び出した枝のノードをダブルクリック → 別 leaf で `embed-timeline` を編集（保存前・保存後）→ `![[embed-timeline]]` の項目で F2・Enter・Delete・⌘Z・ドラッグ並べ替え → 4 レイアウトに切り替え → SVG／PNG 書き出しと Excalidraw 挿入 → `embed-cycle` を開く → view を閉じる → Mappy を無効化 | コマンドは map view がアクティブなときだけパレットに出る（Markdown 側では出ない）。候補は `mappy: true` のノートだけ（`embed-host`・`heading-document`・自分自身 `free-topics` は出ない）で、ファイル名と親フォルダ `Fixtures` が並び、一致した文字が強調される。原文は `- 習慣化する` の次の行に `- ![[uneven-branches]]`、`  - 睡眠` の次に `  - ![[embed-timeline]]`、`- 用語 B` の次に `- ![[embed-timeline]]` が 1 行ずつ足され、他の行と frontmatter は不変。呼び出したノート（`uneven-branches`・`embed-timeline`）は不変。追加した項目が選択され、インライン入力は開かない。追加した項目は表示側の枠（下記）として描かれる。⌘Z 1 回ごとに 1 項目が丸ごと消えて 3 回で元の原文に戻る。「Wikilinks を使用」がオフでも `![[…]]` のまま、「新しいリンクの形式」の絶対では `![[Fixtures/embed-timeline]]`、相対では同じフォルダなら `![[embed-timeline]]`（サブフォルダなら `![[サブフォルダ/embed-timeline]]`）、最短で同名ノートが 2 つあれば `![[Fixtures/embed-timeline]]` の完全パスになる。一致箇所の強調は Obsidian の `renderMatches` で、`prepareFuzzySearch` が `フォルダ/タイトル` に返す `matches` と同じ位置（実機: LEV-71、`artifacts/lev-71-map-search-e2e/call-map-probe.mjs` と `record.md`。本人の試用で原文が変わっている `free-topics` は使わず、fixture の原文を別名で置いて行う。LEV-83 より前は map view が常に 1 ノードを選択状態に保っていたので、LEV-71 の「何も選択せず」は開いた直後の本体ルートが選ばれた状態だった。ユーザー割り当てのホットキー、同じノートを左に Markdown で開いた分割表示（editor の transaction 経由）、保存中と H2 のないノートの Notice、見出し形式の `### ![[…]]` も同じ probe で確認。候補 0 件の「マップがありません」とモバイルは実機未実施）。未選択→トピック（LEV-83）: 空白のクリックでどのノードにも選択の枠がなくなり（パンでは残る）、呼び出しで原文の末尾に `## ![[embed-timeline]]` の 1 区画だけが足され、frontmatter（`mappy-topics`）と本体は不変。呼び出し先のルート「講座の進行」が link の印付きのトピックのルートとして本体のそばの列（位置未設定の既定配置）に現れ、その子 4 つが読み取り専用の枝として並ぶ。新しいトピックが選択され、インライン入力は開かない。⌘Z 1 回で区画ごと消える。見出しのないノートでも同じくトピックになる（「先に H2 を」は出ない）。トピックのルートを空白で離すと `mappy-topics` に `"![[embed-timeline]]": { mindmap: [x, y] }` が書かれ、ノードの上で離すと `- ![[embed-timeline]]` の枝（区画と項目が消える）、⌘Z で順に戻る。枝の `![[embed-timeline]]` を空白へ離すと `## ![[embed-timeline]]` のトピックと位置になり ⌘Z で枝に戻る。トピックのルートの F2 は原文 `![[embed-timeline]]`、Delete で区画ごと消え ⌘Z で戻る。未選択で矢印キーを押すと本体ルートから選択が再開し、Enter／Tab／Delete では何も起きず、⌘Z は効く。F2 で編集中に空白をクリックすると下書きが保存され未選択のまま（実機: 未実施。Obsidian が本人の試用中のため。jsdom と headless Chrome の `call-map-topic` で確認、`artifacts/lev-83-call-as-topic/record.md`）。表示側: 5 つの項目がそれぞれ呼び出し先のルートの文を表示し、その子が通常の枝として右に並ぶ（`#同じ名前` はその見出しをルートにした部分木、`embed-2000` はルートの子 13 個までが開き、それより下は折りたたみバッジ）。呼び出した部分は控えめな文字色で、項目に `link` の印、hover のツールチップに元ノートのパス。自分自身・文中の埋め込み・`mappy: true` のないノート・存在しないノート・ブロック参照はリンク、画像は画像のまま。明色・暗色テーマで読める。呼び出した部分のノードで Enter／Tab／F2／Delete／ドラッグ／右クリックの編集項目は効かず（Notice「読み取り専用」。右クリックは「元のマップを開く」「折りたたみ」と履歴だけ）、Space と開閉ボタンだけが効いて原文は変わらない。呼び出した部分のどのノードでもダブルクリックで元ノートがマップで開く（§8 のルーティング。LEV-74 で map view が navigation view になったので同じ leaf を置き換え、⌘で新しいペイン、戻るでホストの map に戻る。LEV-74 より前は非 navigation のため新しいタブだった）。別 leaf の編集（未保存でも）で 2 つの枝が更新され、ホストと元ノートの原文は変わらない。呼び出し先が消える・`mappy: true` を失うとリンクに戻る。項目の F2 は原文 `![[…]]` をそのまま編集し、Enter／Delete／ドラッグは通常のノードと同じ差分で、Delete で呼び出した木ごと消え ⌘Z で戻る。4 レイアウトすべてで重なりがなく、左右バランスでは呼び出した木も側に従う。SVG／PNG 書き出しと Excalidraw 挿入に呼び出した木が通常のノードとして入る。`embed-cycle` では `![[embed-nodes]]` が枝になり、その中の `![[embed-cycle]]` はリンク（1 段だけ）で描画が止まらない。view を閉じたあと DOM・イベントが残らず、Mappy の無効化で残らない（実機: 枠の形は LEV-72 で確認済み（`artifacts/lev-72-embed-node-e2e/`）。枝の形の取り直しは Obsidian が本人の試用中のため未実施。LEV-72 に追記して verification を再開する） |
| E36 | FileView 化（LEV-89）: `Fixtures/navigation.md` 相当の `mappy: true` のノートをマップで開き、右サイドバーにアウトライン・バックリンク・プロパティを出す → map のタブをアクティブにする → コマンドパレットで「パスをコピー」「ファイルを削除」「ファイルを移動」「エクスプローラーで表示」「既定のアプリで開く」「ファイル名を編集」「プレビューに切り替え」を検索する → 「パスをコピー」を実行 → 「最近開いたファイル」（クイックスイッチャーの空入力）を見る → ファイルエクスプローラーで map のノートを改名 → ノードを 1 つ編集中（F2）のまま別のノートへ移る → map のノートを削除（履歴のあるタブ／新しいタブで開いた履歴のないタブの両方） → map と Markdown の分割をタブリンクして map で別の map ノートのリンクを踏む → E02 の Escape・E06 のリンク・E20 の ←→・E22 を再確認 → E24 の対話フレームと M10 の埋め込みを開く | map が active な間、サイドバーは map のノートの見出し・バックリンク・プロパティを出し（隣の Markdown タブではない）、パレットに「パスをコピー」〜「既定のアプリで開く」が出て map のノートに作用し、「ファイル名を編集」（F2 の既定）と「プレビューに切り替え」は出ない。F2 は従来どおりノードの編集。最近のファイルに map のノートが載る。改名でタブのタイトル（`<新しい名前> · マップ`）とルートノードが変わり、再起動後も同じノートが map で開く。編集中のノードは別のノートへ移る前に保存される（LEV-74）。削除で履歴のあるタブは前のノートに戻り、履歴のないタブは空の view（他にタブがあれば閉じる）になり、下書きは書き込まれず Notice も出ない。リンクした Markdown ペインは map と同じノートを追い、map ノートなら map で開く（Obsidian のリンクの規則。architecture §6）。Escape で leaf は移らず、リンクは map の leaf を置き換え、←→ で表裏を往復し、トグルで Markdown にした leaf は Markdown のまま。E24・M10 は従来どおり。実機: 2026-09-21、Obsidian 1.14.2、PR #61 のブランチ `40bc3ea` のビルド（`artifacts/lev-89-fileview-e2e/record.md`。LEV-74 の 33 ステップは 32 PASS で、唯一の差は FileView 化で期待が変わった 02。コアコマンドは真 10・偽 2、F2 は inline 編集、最近のファイル・改名・削除（履歴あり／なし）・E24・M10 とも期待どおり、console の error／unhandledrejection 0）。**未実施・未確認として残るのは、リンクしたタブグループ（上の手順の「map と Markdown の分割をタブリンクして…」）、バックリンクの `view.file`（1.14.2 では null）、プロパティのペイン（この Vault では core plugin が無効）。** 確かめた上で見つかった不具合: 「現在のビューをブックマーク」の `checkCallback` が map では undefined で無効（Markdown では真。Obsidian のブックマーク生成が map view を知らない。低優先の既知事項で未起票）。 jsdom は `artifacts/lev-89-fileview/record.md` |
| E37 | 画像の貼り付け（LEV-146。`npm run harness:e2e:paste`）: `Fixtures/E2E-paste-image.md` をマップで開き、ノードを選んで Tab → 画像を貼る → Escape（LEV-203 から、ノードは仮の名前「サブトピック」のまま画像を持って残る） → 別のノードで Tab →（全選択の仮の名前を）Backspace で消す → 画像を貼る → Enter（題名が空のノードを作る。LEV-142・LEV-146 の形） → さらに別のノードで Tab → 画像を貼る → タイトルを入力して Enter | どちらの貼り付けでもエラー行も Notice も出ず、**貼った瞬間にそのノードの上に画像が見える大きさで描かれ**（編集中でも。`shownImages`）、画像が本文に書かれ、最後のノードにタイトルが付いて入力欄が閉じる。1 回目のノードは「サブトピック」のまま画像を持って残り（LEV-203）、2 回目のノードは「空のノード」になる（画像を貼ったノードは題名が空になるので、2 回目は必ず「同名ノードが複数ある」状態で起きる。ここで下書きがノードを見失うのが LEV-142・LEV-146 の症状）。実機: 2026-09-22、Obsidian 1.14.2、`artifacts/lev-146-same-name-ids/`（修正前の 0.3.2 のビルドで再現＝`e2e-before-fix.json`、修正後は PASS＝`e2e-after-fix.json`。編集中の表示は `e2e-visible-before.png`／`e2e-visible-after.png`）。同じ行列を jsdom で常時回すのは `tests/ui/mindmap-view-paste.test.ts` |
| E38 | レイアウト切替とトピックの位置（LEV-129）: `Fixtures/free-topics.md` をマップで開き、(1) 空白のダブルクリックで出したトピックに名前を付けずに Escape → 左下で別のレイアウトに切り替える（LEV-203 から、作ってすぐの Escape はトピックを取り消す。名前が付く前の切替は入力欄を開いたままボタンを押す形になり、押下の blur が仮の名前「トピック」のまま確定する。jsdom の LEV-129 のテストは入力欄を開いたままの切替に直した）、(2) 通常マップ⇄タイムラインを往復して各レイアウトで置いた位置を見る、(3) トピックをドラッグして離し、見た目の位置と frontmatter `mappy-topics` の座標を突き合わせる、(4)（任意）ドラッグを握ったまま切り替える | (1) トピックはダブルクリックした場所に留まる（修正前は本体の下の既定位置へ飛ぶ）。(2) レイアウトごとの位置が保たれる。(3) 離した位置と保存された座標が矛盾しない。(4) 切替の瞬間も木がポインターの掴んだ点に留まり、切替自体は移動として保存されない。実機: 2026-09-22 に (1)〜(3) を本人の手操作で PASS（`artifacts/lev-129-realdevice-2026-09-22/record.md`。main `7ce49bf` から `npm run harness:prepare` で配置したビルド）。**(4) は実機未実施**——マウス 1 つではドラッグを握ったまま左下のレイアウトボタンに届かない（テストのコメント「No button reaches a held pointer, but `setState` switches layouts」）。**frontmatter の `mappy-layout` を書き換えても開いている view のレイアウトは変わらない**（ノートの変更では本文を読み直すだけ）ので、その手順では (4) を確かめられない。手で届く経路は、ドラッグを握ったまま console／CDP から遅延付きで `leaf.setViewState` に `layout` を渡す形（`setState` → `applyMode`）で、実機では未実施。`scripts/e2e/` には未登録で、(1)〜(3) の根拠は本人の手作業の record だけ（再実行できる形にするのは残り。product-plan H0 の残項目）。(4) の自動テストは jsdom（`tests/ui/mindmap-view-topics.test.ts` のうちドラッグ中の切替を扱う 4 件）と headless Chrome の撮影（`topic-drag-layout-switch`）。左下のボタンを 2 本目のポインター（タッチ）で押す経路（LEV-182: 切替が頼む Fit はドラッグが終わるまで保留）は jsdom の LEV-182 の 9 件と headless Chrome の `topic-drag-layout-button-touch`・`body-drag-layout-button-touch` で、実機（タッチ画面）は未実施|
| E39 | 左下のレイアウトボタンと同時の操作（LEV-196・LEV-150 のレイアウト切替の分。`npm run harness:e2e:layout-switch`）: `Fixtures/E2E-layout-switch.md`（通常・空題名 2 つ・同名 2 つ・トピック）で、(1) 下書き: F2 → 入力 → レイアウトボタンをクリック（押下の blur で下書きが確定し、クリックで切替が書く）×通常・空題名・トピック、(2) キー: ボタン → Enter／Tab／Delete／F2 と確定、および画像の貼り付け ×同じ 3 形（クリックでフォーカスはボタンに移り、人の手ではノードを押し直してキーを打つまでに再読込（約 60 ms）が終わるので、キャンバスへのフォーカスはスクリプトで戻して機械の速さで送る。競合そのものを固定する行で、人がマウスで通る経路ではない）、(3) 折りたたみ: 2 つ目の同名・2 つ目の空題名を選んで折りたたみ → ボタン | どの行も Notice もエラー行も出ず、操作の編集と `mappy-layout` の両方が原文に入る。折りたたみと選択は同じノードに残る。修正前（`processFrontMatter` がマップの保存経路の外で書き、再読込まで `document.source` が古い）は 20 行すべてが「Markdown が変更されています」「Markdown が更新されました…」「ノートが更新されました。画像の追加をもう一度実行してください。」で拒否されるか、折りたたみと選択が外れた（下書きの行は競合のタイミング次第で保存が先に通ることもあるが、そのときも Notice か下書きの残りで FAIL する）。各行は原文が期待どおりになって map が読み直すまで待ち、3 秒で届かなければ FAIL（直後の map は書き込み前の原文のままでも「最新」に見えるため）。実機: 2026-09-26、Obsidian 1.14.2、`artifacts/lev-196-layout-switch/`（修正前 `before-fix-r3b.json`、修正後 `after-fix-r3-run*.json`）。指で運ぶトピック・本体のドロップ（LEV-182 のタッチ）は、マウス 1 つではボタンに届かず、デスクトップの CDP のタッチ入力ではマップのドラッグが始まらないので実機では未実施。jsdom の `tests/ui/mindmap-view-layout-write.test.ts` が全行（ドロップ・Undo・E05・マップでないノート・再読込をまたぐ画像の追加を含む）を固定する |
| E40 | ノード内の改行（LEV-202）: リストのノート・見出しのノートで、通常ノード・本文のルート・トピック・Tab で作ったノード（LEV-203 から仮の名前「サブトピック」が全選択で開き、打った文字で置き換わる）を F2 で開き、(1) Shift+Enter で改行して Enter、(2) もう一度 F2 → そのまま Enter、(3) 日本語 IME の変換中に Enter／Shift+Enter、(4) 複数行の文を貼り付けて Enter、(5) ⌘Z → ⌘⇧Z、(6) Markdown 側で `<br>` を書く、(7) `[ ] …` のような拒否される名前で Enter → ノードをダブルクリック | (1) 入力欄の中で改行し、確定で `- 温泉<br>旅行` のように原文の 1 行へ `<br>` で書かれ、マップは同じ位置で改行して表示。(2) 入力欄に改行で戻り、原文は変わらない（`<BR/>` などの書き方もそのまま。1 行だけ直しても改行の数が同じならタグは元のまま）。(3) 変換の確定だけでノードも改行も増えない。(4) 改行が `<br>` になる。(5) 1 回で戻り 1 回でやり直す。(6) マップに改行で現れる。Obsidian の閲覧モード・ライブプレビューでも改行で表示される。(7) エラー行と下書きが残る。`\` の直後・インラインコードの中の改行は拒否（下書きは残る）。複数行の Setext 見出しは、編集すると 1 行にして改行を `<br>` で書く（Obsidian はこの形を見出しとして読まないため）。`npm run harness:e2e:line-break` が回すのは (1)（リストのノートの項目・本文のルート・トピック・Tab の空のノード、見出しのノートの ATX・1 行の Setext）・(2)・(4)・(5)・(6)（外部の書き込みと Obsidian 自身の描画）・(7)（ダブルクリック、`\` の直後）、複数行の Setext の 1 行への書き込み。(3) の IME と、閲覧モード・ライブプレビューの画面での見え方は人の手で見る。ブラウザ検証ページでは `line-break-*` の 3 ケース（実キーの Shift+Enter・再編集・SVG 書き出し） |
| E41 | 削除後の選択（LEV-204。`npm run harness:e2e:delete-selection`）: 専用 test-vault の Obsidian で、報告と同じ形のノート（親の子 3 つの末尾を消す）・トピックが 1 つのノート・H2 のない本体のノート・見出し形式のノートを 4 レイアウトで開き、各ノードを選んで Delete／Backspace（実キー）→ ⌘Z。最後に選択先が画面の外にある状態で Delete | 選択は 1 つ上の兄弟 → 1 つ下の兄弟 → 親（原文順。フリートピックのルートはトピック同士が兄弟で、残りがなければ本体のルート。仮想ルートは選ばず、同じ種類が残らなければ最上位で最も近いノード）。選択先は表示の中に入り、フォーカスはマップに残る。⌘Z で原文がバイト一致して消えたノードが戻り、選択は削除で移したノードに残る。実機: 2026-09-26、Obsidian 1.14.2、専用 test-vault、rebase 前のコミット `8bfd2bb`（当時の main `254c98e` の上。レビュー 2 回目の対応まで）のビルドで PASS（76 行 = Delete 38・Backspace 38、画面外 1、失敗 0）。修正前のビルド（`src/core/commands.ts`・`list-commands.ts` だけ origin/main に戻したもの）では FAIL（失敗 114）: 56 行が親（仮想ルートの下では view の既定の先頭ノード）を選び、画面外の行も親が選ばれ表示の外。修正前と同じ選択になるのは一人っ子・一つだけのトピック・本体のルート・H1 区画 2 つの 20 行で、規則の「親」側を固定する行。レビュー 3 回目の修正（見出し形式で、区画の削除が上の段落と下の Setext 見出しをつなぐときに空行を残す。選択先は削除後の解析で `from` から引く）を含み、main `6ba33ed`（LEV-202 の merge まで）を取り込んだ merge コミット `115eb67` のビルド（`main.js` sha256 `8ce12304…81e1d9`）でも 2026-09-26 に再実行して PASS（76 行 = Delete 38・Backspace 38、画面外 1、失敗 0）。Setext がつながる形そのものは実機の行列に入れておらず、core のテストで固定する。証跡は `artifacts/lev-204/record.md` |
| E42 | タイムラインの同じ側のステージの間隔（LEV-205。`npm run harness:e2e:timeline-stage-gap`）: `tests/fixtures/timeline-stages.md` を `Fixtures/E2E-timeline-stage-gap.md` に書いてタイムラインで開き、下側の Section2 の森（末端「ビジランス効果」「ポモドーロ」）の右端（ノードと開閉ボタン）から、同じ下側の Section4 の縦線までを layout px（画面の px ÷ ズーム）で測る | 距離が `TIMELINE_STAGE_CLEARANCE`（72px、±1.5px）で、開いただけではノートを書き換えない。`--clearance <px>` は別の値でビルドしたときの比較撮影用。headless Chrome の撮影 `timeline-stage-gap` は同じ距離を view と Excalidraw に挿入する scene の両方で測る（実機: 2026-09-26、Obsidian 1.14.2、この worktree 専用の test-vault と CDP 9235 で PASS（71.6px、原文不変）。修正前相当の 24px のビルドでは FAIL（23.6px）。`artifacts/lev-205-timeline-stage-gap/record.md`） |
| E43 | 新しいノードの入力欄（LEV-203。`npm run harness:e2e:new-node`）: `Fixtures/E2E-new-node.md`（リストのノート。本文のルート・ステージ・通常ノード・トピック）で、4 レイアウトそれぞれ (1) 通常ノード・本文のルート・トピック・ステージを F2 で開き、空・1 文字・折り返す長さ（全角 30 文字）・2 行（Shift+Enter）にして Enter → F2 → Escape、(2) Tab（子）・Enter（兄弟）・空白のダブルクリック（トピック）でノードを作り、すぐ Escape → ⌘Z／そのまま Enter、(3) Tab で作った仮の名前の上で日本語 IME の変換を始めて確定 → Enter | (1) 入力中・確定後・再編集・Escape 後でノードの外形（配置位置と倍率 1 の幅・高さ）が 0.5px 以内で同じで、マップがずれない。入力欄は行数ちょうどの高さ（1 行 = line-height）でノードの文字の枠の上端から始まり、カーソルは行の縦の中央。空のノードは確定後も編集中も幅 96px（`.is-empty`／`.is-draft-empty`）・1 行の高さ。(2) 「サブトピック」「トピック」が全選択で開き、確定後も同じ外形。打たずにすぐ Escape でノードが消えて原文は元のまま、⌘Z・⌘⇧Z で変わる手順も残らず、追加で開いた折りたたみ・表示位置・選択も戻る（jsdom）。トピックのルートでの Enter のように追加がフリートピックになるときは「トピック」（jsdom）。そのまま Enter で仮の名前のまま確定。既存ノードの Escape は編集の取り消しだけで、新しいノードでも、打ってからの Escape、画像を貼るなど作成のあとの書き込み（書き込み中を含む）、保存中の Escape はノードを残す。画像やリンクだけを持つ題名が空のノードは従来どおりその中身の箱で、空の行も 96px の最小幅も付かない（`new-node-image-only`）。(3) 変換の開始で仮の名前が置き換わり、確定した文字が書かれる（CDP の `Input.imeSetComposition`。OS の IME そのものは人の手で見る）。実機: 2026-09-26、Obsidian 1.14.2（Electron の Chromium 124.0.6367.243）、`artifacts/lev-203-new-node-input/`（修正後 PASS＝`e43.json`。修正を戻したビルドで FAIL＝`e43-reverted.json`: 空のノードが編集中 24×34・確定後 24×28、空のトピックが 41×54 → 40×28、Escape で `- ` が残る）。headless Chrome 153 では入力欄が空のノードからはみ出す形は出ない（Chromium 124 は `min-width: max(40px, 100%)` の 40px をノードの幅に数えなかった）ので、実機でしか見えない差がある。ブラウザ検証ページでは `new-node-*` の 10 ケース、jsdom では `tests/ui/mindmap-view-new-node.test.ts` |
| E44 | レイアウト切替をまたぐ Undo／Redo（LEV-206。`npm run harness:e2e:layout-switch` の `undo-*`・`redo-*` の行）: `Fixtures/E2E-layout-switch.md` で、(1) F2 で改名 → 左下のボタン（タイムライン）→ ⌘Z → ⌘⇧Z、(2) F2 で改名 → ⌘Z → ボタン → ⌘⇧Z → ⌘Z × 通常・空題名・同名（2 つ目）・トピック | どの行も Notice もエラー行も出ず、⌘Z で改名が消え、⌘⇧Z で戻る。どの段の原文も `mappy-layout: timeline` を持ち（切替そのものは Undo の 1 段にならない）、それ以外は改名前・改名後の原文とバイト一致し、マップはタイムラインのまま。修正前は切替の書き込みが履歴を捨て、(1) の ⌘Z、(2) の ⌘⇧Z が何もしない。外部変更のあとに履歴を捨てる規則は E05 のまま。実機: 2026-09-26、Obsidian 1.14.2、この worktree 専用の test-vault と CDP 9236 で PASS（E39 の 20 行と合わせて全行）。`src/obsidian/document-store.ts` だけ origin/main に戻したビルドでは E44 の 8 行すべてが FAIL（⌘Z／⌘⇧Z が何もしない）、E39 の行は通過。証跡は `artifacts/lev-206-layout-switch-undo/record.md` |
| E45 | 長文・画像・深い枝が混在する大規模文書のタイムライン（LEV-20、E10 のタイムライン版。`npm run harness:e2e:timeline-large`）: `makeMixedFixture`（`scripts/performance-fixtures.mjs`。約 40 ノードに 1 つのステージで、16 段の一列の鎖・長い日本語と画像・平らな兄弟・長短混在と途中の画像（Vault・Markdown 形式・欠落）を順に回し、枝のないステージと画像つきのステージを混ぜる）の 500／2,000 ノードを `Fixtures/E2E-timeline-large-<N>.md` に書いてタイムラインで開き、画像を読み終えて位置が止まってから測る。続けて第 4 ステージを ⌥↓ → ⌥↑、第 1 ステージの鎖を段 8 → 段 4 → ステージの順に実ポインターで閉じ（開閉ボタンにホバーしてから押す）、逆順に開く。各段で同じ測定をやり直す | ノード同士・開閉ボタンとの重なりが 0（layout px、0.5px の余裕）、ステージごとの森の箱が互いに重ならず軸の帯をまたがない。同じ側で隣り合うステージは、次の縦線が前の森の右端（ノードと開閉ボタン）から `TIMELINE_STAGE_CLEARANCE`（72px）−1.5px 以上離れ、少なくとも 1 組はちょうどその値に張り付く（規則そのものに届いていることの確認）。DOM に原文の全ノードが同じ題名で 1 つずつ出る。軸の並びが原文のステージ順と一致し、⌥↓ で原文と軸の両方が入れ替わり、⌥↑ で原文がバイト一致に戻る。ホバーで − が見え、閉じたバッジは全子孫数、再展開で全ノードが戻り、開閉では原文を書き換えない。期待値の 72px はケースに固定（src から読むと定数を戻したとき期待値も動くため）、`--clearance <px>` は別の値でビルドしたときの比較用。開くまで・安定まで・開閉と ⌥↓ の反映・全体表示でのパン／⌘-wheel ズームのフレーム間隔は記録するだけで判定しない（実機: 2026-09-26、Obsidian 1.14.2、この worktree 専用の test-vault と CDP 9237、Mappy 0.3.6 で 500・2,000 とも PASS（重なり 0、同じ側の最小間隔 71.66／71.53px）。`TIMELINE_STAGE_CLEARANCE = 24` のビルドと `VERTICAL_GAP = -12` のビルドではそれぞれ FAIL。2,000 ノードのパン・ズームは wheel ごとに p95 0.7〜1.0 s の描画フレーム（ケースが記録する long task は 0 件で描画・合成の側。タイムライン固有でないことは使い捨てのプローブで一度確認しただけ）→ LEV-213。`artifacts/lev-20-timeline-large/record.md`） |
| E47 | Undo／Redo の前後の折りたたみと選択（LEV-150 の Undo／Redo の分。`npm run harness:e2e:undo-ids`）: `Fixtures/E2E-undo-ids.md`（通常・空題名 2 つ・同名 2 つ・トピック）で、⌘Z で戻す段（`子1` を F2 で同じ長さの題名に改名・長い題名に改名・Delete。後の 2 つは後ろのノードの位置を動かすので、段の編集集合が正しいときだけ id が引き継がれる）×（(1) 対象のノードを選択して折りたたむ → ⌘Z → ⌘⇧Z、(2) 左下のボタン（タイムライン）→ 選択して折りたたむ → ⌘Z → ⌘⇧Z）× 通常（親）・空題名（2 つ目）・同名（2 つ目）・トピックの 24 行と、2 つのマップ（同じノートを上下の分割で開き、2 つ目のマップで折りたたみと選択、1 つ目で長い改名 → レイアウトのボタン → ⌘Z → ⌘⇧Z）× 空題名・同名の 2 行。⌘Z／⌘⇧Z は、クリックでキャンバスの中に残ったフォーカスのまま実キーで送る（空白をクリックしてフォーカスを戻すと選択が外れるため。フォーカスがキャンバスに無ければ送らずに止まる） | どの行も Notice が出ない。⌘Z・⌘⇧Z の原文が段の前後（(2) は `mappy-layout: timeline` 付き）とバイト一致する。対象のノードの id（マップの解析での題名と順番で特定）が変わらず、折りたたまれたまま選択されている（2 つのマップの行は 2 つ目のマップで見る）。実機: 2026-09-26、Obsidian 1.14.2、この worktree 専用の test-vault と CDP 9239 で 26 行 PASS。`src/ui/mindmap-view.ts` と `src/obsidian/document-store.ts` を origin/main に戻したビルドでは、空題名・同名の 12 行と 2 つのマップの 2 行が FAIL し（id が変わり、折りたたみと選択が外れる）、通常・トピックの 12 行は通過した。2 つのマップは上下に分割する（左右だと 1 つ目のマップのノードが 2 つ目の下に残り、実クリックが届かない）。証跡は `artifacts/lev-150-undo-redo-ids/record.md` |
| E48 | 明色・暗色テーマ × テーマ設定（LEV-14。`npm run harness:e2e:theme`）: `Fixtures/E2E-theme.md`（リンク・画像・折りたたんだ枝）をマップで開いたまま、Obsidian の基本テーマ（`app.changeTheme`: moonstone／obsidian）と Mappy の設定タブの「テーマ」（Obsidian に従う／明色／暗色。ドロップダウンを操作）の 6 通りに切り替え、各状態でノードを選び、歯車のポップオーバーと F2 の入力欄を開く | マップの地（キャンバス）が設定どおりの明暗になり、`theme-light`／`theme-dark` の class は明示指定のときだけ付く。前景と背景を実際に重なる色（半透明の背景は祖先まで合成し、前景には要素と祖先の `opacity` を掛け、描かれていない輪郭（`outline: none`・幅 0）は地の色とみなす）で読んだ WCAG のコントラストが、ノードの文字・ルートの文字・折りたたみの件数・ズームの表示・ポップオーバーの題と説明・入力欄の文字で 4.5 以上、線・ノードの枠・選択の輪郭・浮かせたボタンのアイコンで 3 以上。リンクは Obsidian 自身のリンク色と地の比（同じ明暗の「Obsidian に従う」状態で body から読む。既定の明色は 4.26）を下回らない（上限 4.5）。明示テーマの各要素の色が、同じ明暗の Obsidian テーマに「従う」ときと 1 チャンネル 4/255 以内で一致する（styles.css の `:where(.mappy-view.theme-light, …)` の変数対応）。原文は変わらない。実機: 2026-09-26、Obsidian 1.14.2、この worktree 専用の test-vault と CDP 9240 で PASS（比べた 52 組すべて差 0）。styles.css の変数対応を消したビルドでは FAIL（アプリと逆の明示テーマの 2 状態で、地の明暗・ルートの文字 2.13／1.34・全要素の色の不一致。失敗 31）。選択の輪郭を `outline: none` にしズームの操作を `opacity: 0.35` にした styles.css でも FAIL（輪郭 1.0、ズームの表示 2.12）。見た目の良し悪し・コミュニティテーマ・OS の「システムに従う」の切替は人の手で見る（撮影は `--shot`）。証跡は `artifacts/lev-14/record.md` |
| E49 | フリーツリーのドラッグ中にビューポートが変わる（LEV-194。`npm run harness:e2e:drag-viewport`）: `Fixtures/E2E-drag-viewport.md`（`mappy: true`、本体・トピック 2 つ。どちらのトピックも `mappy-topics` に位置を持つ）を通常マップで開き、表示を x 120・y 120・90% に置いて、トピック「資料」・本体のルートを実マウスで押して 60 × 30 px 運び、ボタンを握ったまま・ポインターを止めたまま、`none`（何もしない対照）・ホイールのパン（deltaY 100）・横のパン（deltaX −60・deltaY 40）・Ctrl＋ホイールの拡大・⌘＋ホイールの縮小（`Input.dispatchMouseEvent` の `mouseWheel`）・右下の 拡大・縮小・100%・全体表示を 2 本目のポインターでタップ（`Input.dispatchTouchEvent`）→ さらに 30 px 運ぶ → 離す、の 2 × 9 = 18 行。`--only <行>` で一部だけ回す | 掴んだ点（ルートの左上からのポインターの位置を layout px で）が操作の前後で 1 px 以内に同じで、木はポインターを中心に拡大・縮小する。運んでいない木はホイールのパンの量だけ、ズームはポインター（ホイール）かキャンバスの中央（ボタン）を中心にその倍率だけ動く（全体表示はビューポートが変わったことだけを見る）。続く 30 px で運んでいる木だけが 30 px 動く。離したあと、保存と読み直しを経て、すべてのルートが離した時点の画面位置に 1 px（または倍率）以内で描かれ、Notice もエラー行も出ない。修正前は、トピックはホイールの量だけ・ズームで別の位置へポインターから外れ、本体は次の移動で押した時のビューポートに書き戻されてパン・ズームが失われる。実機: 2026-09-26、Obsidian 1.14.2、この worktree 専用の test-vault と CDP 9241 で、レビュー 3 回目の対応まで入れたコミット `fcbd253` のビルド（`main.js` sha256 `65dc2909…`）で 18 行 PASS（それより前の 2 つのビルド `b837fe7e…`・`e792a5f2…` でも 18 行 PASS）。`src/` を origin/main に戻したビルド（`99ce0ab7…`）では 16 行が FAIL し、`none` の 2 行だけが通る（対照）。タップはデスクトップの Electron に CDP で送ったタッチで、タッチ画面の端末そのものではない。jsdom は `tests/ui/mindmap-view-topics.test.ts` の LEV-194（同じ行列＋保存中の窓へのホイール）と `tests/ui/node-drag.test.ts` の LEV-194（ゴーストの掴み位置・元の位置の箱）。証跡は `artifacts/lev-194-drag-viewport/record.md` |
| E50 | 別ウィンドウ（LEV-14、E09 の別ウィンドウ。`npm run harness:e2e:popout`）: `app.workspace.openPopoutLeaf` で開いた popout にマップを開き、popout 自身の CDP ターゲットへ実クリック・実キーを送る。(1) 描画 (2) 選択 → F2 改名 → Tab で子を追加 (3) 歯車のポップオーバーを開いてキャンバスの空白を押す (4) ズームボタン・ウィンドウの縮小 (5) Obsidian を暗色へ (6) main のマップを `moveLeafToPopout`（タブのメニュー「新規ウィンドウに移動」と同じ API）で移して F2 改名 (7) 下書きを開いたままウィンドウを閉じる (8) popout の開閉 × 10 | (1) ノードが popout の document にあり、Mappy のスタイルが popout の head に入って効く (2)(6) 原文がその行だけ変わる (3) ポップオーバーが popout に開いてフォーカスを持ち、外側の押下で閉じる (4) ズームの表示が変わり、キャンバスがウィンドウに合わせて縮む (5) 先に明色へ切り替えて popout が明色になったことを確かめてから暗色へ。popout の body とキャンバスが暗色 (7) ウィンドウが閉じ、原文は元のままか下書きがそのまま入ったかのどちらか（どちらだったかを record の `outcome` に残す。どちらにするかは LEV-215） (8) すべて閉じる。最後にハンドラー数が開く前と一致し、popout にいた view がすべて回収され、ウィンドウは 1 つに戻り、どのウィンドウでもページエラーがない。実機: 2026-09-26、Obsidian 1.14.2、CDP 9240 で PASS（7 は `saved`）。ポップオーバーの blur リスナーをグローバルの `window` に付けて popout の `win` から外すビルドでは FAIL（ポップオーバーを開いた popout の view が残留）。外側押下のリスナーを main の document に付けるビルドは PASS のまま（フォーカスが外れたら閉じる別の経路で閉じるため、3 はこの壊れ方を区別しない）。6 で Enter が兄弟の追加（「サブトピック」）も起こしたことが通し 6 回中 1 回あった（LEV-216）。原因は 2 つに切り分けた: 下書きの途中でウィンドウが OS のフォーカスを失うと blur で確定して閉じ、戻って押した Enter が選択ノードに届く製品の欠陥（修正し、E56 で固定）と、ケースが送っていないキーが届くこと（popout は開くたびに OS のフォーカスを取るので、その間に人が打ったキーが入る。LEV-216 の計測中に Space・⌘・⌥・↓ が届いた）。6 はウィンドウが受けたキーとウィンドウの blur／focus を record の `keys` に残し、F2・Enter 以外のキーが届いたら「ケースが送っていないキー」として FAIL を分けて出す。ページエラーの一覧は実行ごとに空から始める（LEV-216 まで、同じ Obsidian の前の実行のエラーが後の実行すべてを FAIL にしていた）。モニターをまたぐ移動・倍率の違う画面・手でのタブの引き出しは人の手で見る。証跡は `artifacts/lev-14/record.md` |
| E51 | 開閉 50 回・プラグイン再読込・ウィンドウ再読込（LEV-14、E09 の開閉・再読込。`npm run harness:e2e:view-lifecycle`）: `Fixtures/E2E-view-lifecycle.md` を新しいタブでマップとして開き、全ノードが並んでから閉じる × 50。5 回に 1 回ずつ、下書きを開いたまま（元と同じ題名）・歯車のポップオーバーを開いたまま・ズームしてから・同じノートの 2 つ目の view を分割で開いてから閉じる。続けてマップを開いたままプラグインを無効化→有効化 × 5、最後に `app:reload`（`--cycles` は 20 以上の整数だけを受け付ける。10 回目と最後の間の増加を測るため） | 閉じたあと `app.workspace`／`app.vault`／`app.metadataCache` のハンドラー数がイベント名ごとに開く前と一致し、`.mappy-view`・ポップオーバー・入力欄・leaf が残らない。閉じた view と `contentEl` の `WeakRef` が、ヒープスナップショットの GC（`HeapProfiler.collectGarbage` だけでは切り離した DOM が残る）のあとすべて回収される。10 回目と 50 回目の間の増加が DOM ノード 200・リスナー 20・ヒープ 2MB 以内。プラグインの無効化で view・コマンド・スタイル・設定タブが消え `setViewState` が Mappy 以前の関数に戻り、有効化で同じ leaf にマップが戻ってそれぞれ 1 つずつ、ハンドラー数も再読込の前と同じ。ウィンドウ再読込でマップが 1 つだけ戻り（後ろのタブとして遅延読み込みで戻った leaf は view state のファイルで見つけて表示する）、再読込の前（開閉とプラグイン再読込の間）と後のどちらにもページエラーがない。実機: 2026-09-26、Obsidian 1.14.2、CDP 9240 で PASS（61 view すべて回収、10→50 回目の増加は DOM ノード +51・リスナー +6・ヒープ −0.7〜+0.4MB）。`layout-change` を `registerEvent` なしで購読するビルドでは FAIL（ハンドラー 4 → 64、61 view すべて残留、DOM +8,995）、ポップオーバーの外側押下リスナーを外さないビルドでは FAIL（ポップオーバーを開いた 10 view だけが残留）。下書きを開いたまま閉じると blur で保存される（コメントは「捨てる」）ことはこの行の範囲外で LEV-215。証跡は `artifacts/lev-14/record.md` |
| E56 | 下書きの途中でウィンドウのフォーカスが外れる（LEV-216。`npm run harness:e2e:window-blur-draft`）: `Fixtures/E2E-window-blur-draft.md` を開き、「通常のノード」を実クリックで選んで F2 → 題名を入力したところで、マップのあるウィンドウの OS のフォーカスを Electron の `BrowserWindow.blur()` で外し `focus()` で戻す（他のアプリへ移って戻るのと同じ。前面のアプリでないと macOS は focus を無視するので `app.focus({ steal: true })` を先に呼ぶ）。(1) main のタブで → Enter (2) `moveLeafToPopout` で新しいウィンドウへ移したマップで → Enter (3) main のタブで → キャンバスの空白を実マウスで押す (4) 移したウィンドウで下書き → ウィンドウの blur → 戻らずにそのウィンドウを閉じる (5) main のタブで下書き → ウィンドウの blur → 戻らずにタブを閉じる（`leaf.detach()`）。ウィンドウの blur が「下書きが active のまま・文書にフォーカスなし」で起きたこと、(1)〜(3) では戻ったあとに文書がフォーカスを持つこと（(4)(5) では持たないこと）を確かめてから数える（起きなければ FAIL。何も確かめていない PASS にしない）。どの手順もウィンドウが受けたキーを残し、ケースが送っていないキーは外部入力として FAIL を分ける | 戻ったあとも下書きが開いたまま・原文は元のまま。(1)(2) Enter で原文がその行の改名だけになり、下書きが閉じる（兄弟が増えない）(3) ウィンドウの中の押下では従来どおり下書きが保存される (4)(5) 背景のまま閉じても下書きが保存される（0.3.7 はウィンドウの blur の時点で保存していた。フォーカスのない文書では外される入力欄に blur が届かないので、ビューが閉じる前に保存する）。ページエラーなし。実機 1.14.2（CDP 9245）で確認済み: 修正を戻したビルド（0.3.7 相当）では (1)(2) が LEV-216 の形（改名＋「サブトピック」の兄弟、その入力欄が開いたまま）、(1)〜(3) が離れた時点で保存されて FAIL。blur の修正だけで閉じるときの保存を外したビルドでは (4)(5) で下書きが失われて FAIL（コードレビュー 1 回目の指摘。Obsidian は `onClose` より前にビューの要素を文書から外すので、閉じる時点のフォーカスでは見分けられない） |
| E53 | フリーツリーを離した直後のフレーム（LEV-197。`npm run harness:e2e:drop-flicker`）: `Fixtures/E2E-drop-flicker.md`（E49 と同じ形。どちらのトピックも `mappy-topics` に位置を持つ）を通常マップで開き、表示を x 120・y 120・90% に置いて、トピック「資料」・本体のルートを実マウスで押して 60 × 30 px 運び、止めて離す。ノートの開き方 3 つ × トピック・本体の 6 行（`slow` の 2 行は各 3 回＝`--repeat`、`alone`・`editor` の対照 4 行は 1 回ずつ、計 10 行）: `alone`（マップだけ。保存は `Vault.process`）、`editor`（同じノートの Markdown エディタを右に分割して開く。保存はエディタの transaction）、`slow`（`alone` で、ノートの読み込みの答えをすべて 80 ms 遅らせる。遅いディスクの代わり）。離す直前から 1.5 s、描かれたフレームごと（アニメーションフレームから積んだタスク）に各ルートの画面位置を読む。あわせて view の再読込の予約と読み込みを epoch 付きで記録し、保存直後の再読込の読み込み中に epoch が進んだか（`superseded`）を行ごとに残す | どのフレームでも、すべてのルートが離した時点の画面位置から 1 px（または倍率）以内にあり、保存が原文を書き、Notice もエラー行も出ない。`slow` では保存直後の再読込が譲られたこと（`superseded`）も確かめ、届かなければ FAIL（競合に届かなかった行を PASS にしない）。修正前は、保存直後の再読込がウォッチャーの新しい予約に譲ると、その再読込が描くまで（約 45 ms）離した木が押した位置へ戻って描かれる。実機 1.14.2 の結果: `alone`・`editor` ではウォッチャー（modify・`editor-change`）の予約が保存の再読込の始まる 0.1〜2 ms 前に届き、`superseded` は毎回 false で、修正前のビルドでも PASS する（この 2 形は、自然な操作ではチケットの順序にならないことの記録。修正の有無は見分けない）。`slow` では 6 回とも `superseded` が true で、修正前のビルド（`main.js` sha256 `65dc2909…`）は 6 行とも FAIL（離して約 90 ms 後から 5〜6 フレーム、トピックは押した位置、本体はトピックが反対側へずれて描かれる）、最終のコミット `62675cd` のビルド（`01b0120e…`）は 10 行 PASS。2026-09-26、この worktree 専用の test-vault と CDP 9242。jsdom は `tests/ui/mindmap-view-topics.test.ts` の LEV-197（トピック・本体 × 切替なし・ドラッグ中の切替）。証跡は `artifacts/lev-197/record.md` |

CDP でキー操作を再現するとき、要素へ送る合成 `keydown` は Obsidian のキーマップを通らない。Obsidian の既定ホットキーと重なるキー（F2 = `workspace:edit-file-title`）は `Input.dispatchKeyEvent` の実キーでも確認する（E02 の F2 は `artifacts/lev-48-f2-scope/f2-probe.mjs`: map view の `Scope` が F2 を受けてインライン編集が開くこと、Escape・入力→Enter・実キーの ⌘Z／⌘⇧Z／⌥↑・編集中の F2・浮かせたボタン上の F2・Markdown editor の F2・Markdown を横に開いた map での F2（編集中・ボタン上を含む）を、各ステップの前後の Markdown のバイト比較と `window` capture 段階での `defaultPrevented` で記録する）。修飾キー付きの実キー（⌘Z など）は、ページが `preventDefault` しないまま通すと macOS のメニューへ転送され、CDP の合成イベントは key equivalent が空の「Obsidian について」に一致してネイティブのダイアログを開き、閉じるまで CDP も応答しなくなる。修飾キーはマップのノードにフォーカスがありインライン入力が閉じているときだけ送る。ディスプレイのスリープや画面ロックの間は Electron のウィンドウが occluded 扱いになり `requestAnimationFrame` が止まりタイマーも 1 秒に絞られるので、map の配置（rAF）を待つ probe は進まず `Runtime.evaluate` が timeout する。`document.visibilityState` が `hidden` なら画面が戻るまで待つか、`artifacts/lev-72-embed-node-e2e/throttling.mjs off` のように `electron.remote.getCurrentWebContents().setBackgroundThrottling(false)` で一時的に rAF を動かし、終わったら `on` で戻す（LEV-72 で発生。描画・入力・撮影は renderer で動くが、実表示は消えたままなので record にそう書く。ウィンドウが他のウィンドウの後ろに回っただけでも同じ `hidden` になる: LEV-71）。Obsidian の `obsidian` モジュール（`prepareFuzzySearch`・`renderMatches` など）はページのコンソールから `require` できない（プラグインローダーがラッパー関数の引数として渡す）ので、実機の関数を probe から呼ぶときは CDP の `Runtime.getProperties` の `[[Scopes]]` で Mappy の bundle の閉包（`var … = require("obsidian")`）をたどる（`artifacts/lev-71-map-search-e2e/obsidian-module.mjs`）。検索モーダルへの入力は `Input.insertText`（IME の確定と同じ経路）で行う。E03〜E05 の組み合わせ（同じノートの 2 leaf・別ウィンドウ・外部変更の直後・フリートピック＋階層図・実キー）は `artifacts/lev-16-multiview/obsidian-multiview-probe.mjs` が各ステップの前後の Markdown を 4 経路（ディスク・`cachedRead`・editor・各 map の解析元）で保存してバイト比較する。

### E2E ケース一覧

実機のケースは使い捨ての probe ではなくリポジトリの `scripts/e2e/` に置き、`npm run harness:e2e:<名前>` で誰でも同じ手順を再実行できるようにする（`scripts/e2e/cdp.mjs` が接続と実キー、`scripts/e2e/case-runner.mjs` が `step`／`check`／`record`／JSON 出力の共通部分、`scripts/e2e/dom-helpers.mjs` が view の DOM 読み取り・マップの解析（`PARSE`／`makeTree`）・ノード選択（中心の最前面の要素がそのノードでなければ押さない。前の手順の Notice ならクリックで閉じて見直し、ペインの外や浮かせたボタンの下なら何があるかを書いて止める）・プラグイン読み込みと fixture ノートを開く手順（同じノートを開いた leaf（map と Markdown。遅延読み込みのタブも view state で見る）があれば開かずに止める）・画像貼り付け・追加（Enter／Tab＋名前。空のノードの書き込みが届いてから名前を打つ）・F2 改名・⌥↑／⌥↓・⌘Z／⌘⇧Z の共通部分。操作のあとは固定時間ではなく原文が変わりマップが読み直すまで待ち、そこから 0.5 秒あとの Notice・下書きを読む（`makeAfter`。変わらないのが正解の手順は上限まで待つ）。操作の前から出ていた Notice は既読の印を付けて数えない。前提の手順（plugin・open など）が失敗したら `required` がそのケースを打ち切り、record に `stopped` を書く。E17・E18 は打ち切っても自分が開いたノートを片付ける（LEV-17）。`scripts/e2e/excalidraw-helpers.mjs` が E24・E25 の共通部分: fixture ノートと図面（Excalidraw の `create`）、プラグインの切替（`makeToggle`。`community-plugins.json` を書き換えず、プラグインと `ExcalidrawAutomate`・drop hook が揃うまで待つ）、ファイルエクスプローラーから図面への合成ドラッグ（`makeFileDrag`。`cdp.mjs` の `once` で `Input.dragIntercepted` を待つ。図面の要素の id と version が静かになるまで前後で待つ）、前のビルドが残した routing の検出（`makeNoStaleRouting`）。開いている leaf のガードは `dom-helpers.mjs` の `refuseOpenLeaves` を共有する（LEV-22）、各ケースはその上に手順と合否を持つ）。`artifacts/` に書いた probe は次のセッションから再実行されず、LEV-142 の修正が「実機で確認済み」として 0.3.2 で出たあと、同じ症状が 0.3.2 のビルドで再現した一因になった（LEV-146）。`npm run harness:e2e`（`scripts/e2e/run.mjs`）は下の表のケースを登録順に 1 つの Obsidian インスタンスへ、ケースごとに新しい接続で走らせる（実機は 1 台なので同時に 1 本、docs/linear-workflow.md「Obsidian 実機は1台なので、実機を使うチケットは同時に1本にする」）。各ケースは独立した子プロセスとして spawn する: 1 本が CDP 呼び出し中にハング・クラッシュしても、その接続だけが失われ、次のケースは新しい接続からやり直せる。`--case <名前>` で 1 本だけ実行（このときは前回の全体実行の `summary.json` を 1 ケース分の結果で上書きしないよう書かない）、`--json <dir>` で各ケースの JSON と、まとめて走らせたときだけ `summary.json` を書く。`--shot <dir>` は撮影に対応するケースだけ PNG を残す。

| 名前 | 対応する実機ケース | 何を固定しているか（AGENTS.md: 回帰テストは何を固定しているか明記する） |
| --- | --- | --- |
| `add-delete` | E02 | Enter（兄弟）・Tab（子）の追加とタイトル確定、Delete の往復が原文とバイト一致し、リストの中に空行を残さない（LEV-75） |
| `delete-selection` | E41 | 削除後の選択が 1 つ上の兄弟 → 1 つ下の兄弟 → 親になる（LEV-204）。Delete／Backspace の実キー（行とレイアウトで交互、4 レイアウトで各形 2 回ずつ）× 末尾・中間・先頭・一人っ子の項目、呼び出し項目、フリートピックの中の項目、隣にトピックがあるトピックのルートと一つだけのトピックのルート、本体のルート、H2 のない本体の項目とその隣のトピック、見出し形式の見出しと H1 の区画 × 4 レイアウト。各削除のあとの ⌘Z で原文がバイト一致し消えたノードが戻ること、選択先が画面の外でも削除で表示の中に入ること。規則そのものは `tests/core/delete-selection.test.ts`、view の選択は `tests/ui/mindmap-view-delete-selection.test.ts`（jsdom、4 レイアウト）でも固定する。修正前のビルドで FAIL することを確認済み（失敗 114、76 行中 56 行と画面外。E41 の行） |
| `undo-redo` | E03 | F2 改名 1 件の ⌘Z／⌘⇧Z が、`mappy:toggle-mindmap` での表裏往復を挟んでも二重適用・取りこぼしなく効く（`DocumentStore` の履歴は view をまたいで共有） |
| `deep-branches` | E17 | 8 段のリストで Tab／Enter の追加・F2 改名・⌥↑／⌥↓・親を変えるドラッグ（部分木の全行の再インデント）と ⌘Z が原文とバイト一致し、エディタの Tab／Shift+Tab が 1 秒以内にマップへ反映され、保存後のマップの深さがエディタのライブプレビューと一致する。わざと壊したビルドで FAIL することを確認済み: `shiftedBranch` が先頭行だけずらす（ドラッグ）、⌥ の向きが逆（⌥↓）、map が `editor-change` を購読しない（エディタの 3 手順）（LEV-17） |
| `convert-to-list` | E18 | 旧見出し形式を開いても書き換えず、H6 の下への追加を「見出しは 6 階層までです。」（完全一致）で拒否し、明示変換の原文・構造、⌘Z／⌘⇧Z の往復がバイト一致する。わざと壊したビルドで FAIL することを確認済み: 本文のインデントが 1 段浅い（`validateConversion` は構造が保たれるので通してしまい、バイト比較だけが捕まえる）、見出しの上限が 7、開いたら自動で変換する（LEV-17） |
| `move-parent-text` | E19 | 子リストの前後に本文がある項目で、子を ⌥↑／⌥↓ しても親の後続文章を持ち去らず、境界を越えず、画像追加が子リストを動かさない |
| `paste` | E37 | 画像を貼ったノードが空タイトルのまま残る状態で、同じ操作をもう一度行っても下書きがノードを見失わない（LEV-142・LEV-146 の同名ノード id 継承）。LEV-203 から、本人の操作どおり Tab → 貼る → Escape で「サブトピック」が画像を持って残ること、空題名の形は仮の名前を消して Enter で作ることも見る |
| `new-node` | E43 | 新しいノードの入力欄と確定後のノードの外形が、4 レイアウト × 通常ノード・本文のルート・トピック・ステージ × 空・1 文字・折り返し・2 行で F2・Enter・Escape の前後に変わらず、入力欄が行数ちょうどの高さ。Tab・Enter・ダブルクリックが仮の名前を全選択で開き、すぐの Escape が原文にも Undo にも何も残さず、そのままの Enter が仮の名前で確定し、IME の変換開始が仮の名前を置き換える（LEV-203）。修正を戻したビルド（0.3.5 相当）で FAIL することを確認済み |
| `layout-switch` | E39・E44 | 左下のボタンでのレイアウト切替と同時に始まった下書きの確定・キー操作・画像の貼り付けが、切替の frontmatter 書き込みとの競合で拒否されず、同名・空題名のノードの折りたたみと選択が切替を越えて残る（LEV-196・LEV-150）。修正前のビルド（`processFrontMatter` での書き込み）で 20 行すべてが FAIL することを確認済み。切替の前の改名が切替のあとの ⌘Z／⌘⇧Z で戻り・やり直せ、どの段もレイアウトを保つ（LEV-206、`undo-*`・`redo-*` の 8 行） |
| `undo-ids` | E47 | ⌘Z／⌘⇧Z の前後で、同名・空題名のノードの折りたたみと選択が外れない（LEV-150）。同じ長さの改名・長い改名・削除 ×（そのまま・レイアウト切替のあと）× 通常・空題名・同名・トピック、および同じノートの 2 つ目のマップ。通常・トピックの行は修正前も通る（題名が 1 つしか無いノードは題名で引き継がれる。修正がそれを動かさないことを固定する行） |
| `drag-viewport` | E49 | トピック・本体を実マウスで運ぶ間のホイールのパン・Ctrl／⌘＋ホイールのズームと、2 本目のポインター（タッチ）での 拡大・縮小・100%・全体表示で、運んでいる木が掴んだ点に留まり、離した位置として保存される座標が画面に見えていた位置と一致する（LEV-194）。修正前のビルドで 18 行中 16 行が FAIL することを確認済み（`none` の 2 行は修正前も通る対照） |
| `drop-flicker` | E53 | トピック・本体を実マウスで離した直後から、保存後の原文が描かれるまでのどのフレームでも、離した木が保存前の位置に描かれない（LEV-197）。`alone`・`editor` の対照 4 行は修正前のビルドでも通る（実機ではウォッチャーが保存の再読込より先に届く記録）。読み込みを 80 ms 遅らせる `slow` の 6 行が修正前のビルドで FAIL することを確認済み |
| `window-blur-draft` | E56 | 下書きの途中でウィンドウが OS のフォーカスを失って戻っても下書きが開いたままで、戻って押した Enter が改名だけを書き、兄弟を足さない（main・新しいウィンドウへ移したマップ）。ウィンドウの中の押下では保存され、背景のまま閉じても（ウィンドウ・タブ）下書きが失われない。修正を戻したビルドで (1)〜(3)、閉じるときの保存を外したビルドで (4)(5) が FAIL することを確認済み（LEV-216） |
| `excalidraw-frame` | E24 | 対話フレームの `mappy: true` のノートがライブのマップ（`mappy-map` の leaf、枠付きのルートと第一階層、線、リンク、枠の中に Fit）になり、通常ノートの枠は Markdown のまま。わざと壊したビルドで FAIL することを確認済み: ルーター（`setViewState` の差し替え）を入れない、ルーターを解除しない（前提確認 `no-stale-routing` で止まる）。前のビルドの wrapper が残った窓でも同じ前提確認で止まる（LEV-22） |
| `excalidraw-lifecycle` | E25 | Excalidraw の後からの有効化・再読込で Option ドロップが効き、Mappy の無効化で hook と `setViewState` の差し替えが外れる（2 通りの積まれ方の両方）。Excalidraw が無効でもマップが開く。Mappy を外した段では hook の枠が Mappy 以前の値に戻ることも見る（段 5 のあとに他のプラグインの hook に見立てた目印を置き、段 6 で Mappy が断ったドロップがそこへ届くこと、段 7 の無効化で枠がその目印に戻ることを確かめ、最後に取り除く）。わざと壊したビルドで FAIL することを確認済み: ルーターを解除しない、hook を解放しない、`layout-change` で再フックしない、`patchMethod` が覆われていても元の関数に戻す、解除で枠を空にする（LEV-22） |
| `theme` | E48 | 明色・暗色の Obsidian テーマ × テーマ設定 3 通りで、マップの地の明暗・`theme-*` の class・主要な要素のコントラスト（文字 4.5、線・枠・輪郭・アイコン 3、リンクは Obsidian 自身の比）と、明示テーマが同じ明暗の Obsidian テーマと同じ色になること（styles.css の変数対応）。変数対応を消したビルドで FAIL することを確認済み（LEV-14） |
| `view-lifecycle` | E51 | 開閉 50 回（下書き・ポップオーバー・ズーム・分割の 2 つ目を含む）・プラグイン再読込 5 回・ウィンドウ再読込のあと、ハンドラーが増えず、閉じた view が回収され（`WeakRef`）、10→50 回目で DOM・リスナー・ヒープが増え続けず、再読込でマップ・コマンド・スタイル・`setViewState` の差し替えが 1 つずつに戻る。`registerEvent` なしの購読・ポップオーバーのリスナーの解除漏れのビルドで FAIL することを確認済み（LEV-14） |
| `popout` | E50 | popout で描画・実キーの改名と追加・ポップオーバー・ズーム・ウィンドウの大きさ・テーマが効き、main から移したマップも動き（ケースが送っていないキーが届いたら外部入力として分けて FAIL、LEV-216）、閉じたウィンドウの view が回収されハンドラーが戻る。popout でだけ漏れるリスナー（グローバルの `window` に付けて popout の `win` から外す）のビルドで FAIL することを確認済み（LEV-14） |

**壊したビルドの検証は、変種ごとに Obsidian を起動し直す（LEV-22）**: 解除しない変種（ルーターの解除なし等）は、prototype の `setViewState` に wrapper を残したまま unload され、同じ窓で次の変種を試すとその残骸が routing を続ける。LEV-22 では 1 窓で 5 変種を続けて回したところ、2〜4 番目にも「ルーティングが残る」が出て、5 番目（ルーター未導入）は PASS した（どれも前の変種の残骸によるもの）。`--reload` はプラグインを読み直すだけで prototype は元に戻らない。変種ごとに窓を終了して起動し直すと、5 変種とも狙った段だけで FAIL した。正しいビルドに戻したあとも、起動し直してから PASS を確かめる。

**revert 検証（LEV-155）**: `add-delete`・`undo-redo` が LEV-140・LEV-142・LEV-146 の再発を捕まえないことは、実際に revert したビルドで確かめた（結論は以下にインライン化する。gitignore された `artifacts/` の JSON だけを根拠にしない——LEV-142 が 2 度出荷された一因は、この形の使い捨て証跡が次のセッションから読めなかったこと）。

孤立した一時 git worktree（このリポジトリの `1c85b86`＝`release: 0.3.3` タグ相当のコミットから作成、push・共有していない）で `git revert -m 1 --no-edit` を新しい順に適用: `2cd53d0`（LEV-146 merge）→ `b990443`（LEV-142 merge）→ `d3c716c`（LEV-140 merge）。LEV-142 も併せて戻したのは、`src/ui/mindmap-view.ts` の `DraftBase.anchor` が LEV-140 の追加した `DraftBase` 型に直接依存しており、LEV-142 を残したまま LEV-140 だけを revert すると型が矛盾してビルドできないため（LEV-146 は LEV-142 の上にさらに積まれているので、これも同時に戻る）。`node esbuild.config.mjs production` でビルドし、この worktree の `test-vault/.obsidian/plugins/mappy/main.js` を差し替えて `--reload` で再読込、Obsidian 1.13.7・2026-09-22 で実行した結果:

- `add-delete`（E02）: **PASS**（落ちない）
- `undo-redo`（E03）: **PASS**（落ちない）
- `paste`（E37、対照実験）: **FAIL**——「編集していたノードが Markdown 側で見つかりません。マップでノードを選び直してください。」（`NODE_GONE_MESSAGE`、`src/ui/mindmap-view.ts`）で落ち、LEV-142・LEV-146 が直した症状そのものが再現した。

`add-delete`・`undo-redo` が PASS のままなのは、この 3 チケットが直した経路（編集中の貼り付け・同名ノードの id 再割り当て）をそもそも通らないためで、この 2 ケースはその再発を捕まえない。範囲は見直さず、この 2 ケースは LEV-75（Delete の空行制御）と `DocumentStore` の履歴・表裏切替という別の実在する欠陥を固定する役割のまま残し、LEV-140/142/146 の再発検知は `paste` が対照実験で示したとおり引き続き `paste` が担う。元の main.js（`npm run package` の成果物）に戻し、4 ケースとも PASS することを再確認して一時 worktree は削除した。

ポートと vault は `MAPPY_E2E_PORT`・`MAPPY_E2E_VAULT` で差し替えられるので、worktree ごとに `artifacts/obsidian-profile`（`obsidian.json` に vault を 1 つ書く）と別ポートで専用の Obsidian を立てられる。新しいプロファイルで開いた vault は制限モードで始まりプラグインが読み込まれないので、その窓で `app.plugins.setEnable(true)` を 1 度実行する（初回はさらに「この保管庫の作成者を信頼しますか？」のダイアログを「作成者を信頼しプラグインを有効化」で閉じる）。新しいプロファイルは /Applications の版（installer 版）で起動するため `minAppVersion` に届かないことがある。既存プロファイルの `obsidian-<版>.asar` をコピーして起動し直すと更新版で開く。

検証用 Obsidian の注意: 検証用 Obsidian は `open -na /Applications/Obsidian.app --args --user-data-dir=artifacts/obsidian-profile --remote-debugging-port=9231` で専用プロファイルと CDP ポート 9231 を付けて起動する。`http://127.0.0.1:9231/json/list` には vault picker（`starter.html`）の target が先に並ぶことがあるので、probe は `type === 'page'` かつ URL が `app://obsidian.md/index.html` で始まる target を選ぶ（`artifacts/lev-71-map-search-e2e/cdp.mjs`）。ウィンドウが最小化されている（画面ロック・他のウィンドウの後ろも同じ）と `requestAnimationFrame` が止まり、map の配置フレームを待つ `Runtime.evaluate` もスクリーンショットも進まない。接続時に `require('electron').remote.getCurrentWebContents().setBackgroundThrottling(false)` を呼んで回避する（LEV-64 の 2 回目の実行が全滅した原因。`connect()` で呼ぶようにした）。描画・入力・撮影は renderer で動くが実表示は消えたままなので、その状態で取った証跡は record にそう書く。

記録テンプレート:

```text
Case ID / 日時:
OS / Obsidian version / Vault / build hash:
入力 fixture / 前提:
操作:
期待する UI / Markdown:
実測 / PASS・FAIL・未実施:
スクリーンショット / 前後の Markdown / 性能記録:
```

`test-vault/Fixtures/` には初回準備時に静的 fixture と 10/100/500/2000 ノードの文書（見出し形式と 5 つの形の H2＋リスト、計 24 ファイル）を生成する。CRLF・末尾改行なし・途中 IME のような条件は、対応するテストで明示的に作る。レイアウトの幾何テストは性能測定の代わりにしない。性能は②の `scripts/browser-harness-perf.mjs`（ブラウザでの解析・配置・描画・入力反映の p50／p95）と、実機の入力から画面反映まで（E10、LEV-33 の `obsidian-realtime-probe.mjs`）を分けて記録する。`node scripts/measure-layout.mjs` は同じ生成文書で `layoutTree` 単体の配置時間（4 モード、展開と折りたたみ、中央値・p95・最大値）を `artifacts/layout-timing/<日時>/` に記録するが、DOM 計測を含まないレイアウト単体の数値であり、§6 の実機計測の代わりにはならない。

### 開発メモリの手順（Obsidian 実機を使わない）

`npm run harness:e2e:memory-procedure`（`scripts/e2e/memory-procedure.mjs`）。AGENTS.md の開発メモリの箇条書き（`bm tool` を含む行。AGENTS.md はフラットな箇条書きで、節見出しは無い）と `.claude/skills/memory-manager/SKILL.md` に書かれた `bm` CLI の手順を、書かれたとおりに実行して期待どおりに動くかを確かめる。Obsidian も vault も要らないので `npm run harness:e2e`（実機の 8 ケース。うち `excalidraw-frame`・`excalidraw-lifecycle` は Excalidraw を入れて有効にした vault が要り、無ければ `setup` で止まって FAIL になる）には登録せず、単体で走らせる。`bm` が無い環境、`sh`・`zsh` のどちらかを起動できない環境（Linux の CI など）では**終了コード 2 で「実行していない」と言って終わる**（PASS にしない）。

毎回、プロセス ID と乱数を混ぜた名前の使い捨て Basic Memory プロジェクトを一時ディレクトリに作り、最後にノートごと消す。**`mappy-memory` には触らない**（後片付けの後で、使い捨てプロジェクトが消えたことと `mappy-memory` が残っていることの両方を確かめる）。`--keep` で残せる。

何を固定しているか（ステップ名ごと。細部と理由は `scripts/e2e/memory-procedure.mjs` の各ステップのコメント）:

- **手順書のブロックをそのまま実行する** —— SKILL.md のコードブロックを抜き出し、当て先・project・差し込み口だけを差し替えて、`sh`（macOS では bash 3.2）と `zsh`（エージェントの Bash ツールが動く本人のシェル）の両方で実行する。差し込み口には対になっていないバッククォート・`$`・`"`・`)`・`'`（手順書の規則どおり `'\''`）を入れ、文字のまま入ることを見る。
  - `documented-write-note` —「新しいノートを作る」。明示した permalink と `type` のノートができる。
  - `documented-inbox-entry` —「ミスをした直後」。本文が見出しを引用している inbox で、既存が重複も消失もせず、新しい 1 件が既存の後ろ・Relations の前に空行で区切られて入る。
  - `documented-lessons-entry` — lessons への差し込み。最後の節へ足すときの `## Relations` への差し替えも実行する。
- **実行してよいかの 2 段の確認** —— 抜き出したコマンドは、すべての `bm tool` 呼び出しが使い捨てプロジェクトの `--project … --local` を持ち、`--project-id`・`--cloud`・`-p`・`basic-memory` を含まないときだけ実行する。実行時も `PATH` の先頭に身代わりの `bm` を置き、条件を満たさない呼び出しは本物へ渡さずに止める（`shim-refuses-other-projects` が止め方と、正しい呼び出しが本物へ届くことを固定する）。
- **bm の挙動** —— 手順書が根拠にしているもの。
  - `defaults-drop-permalink-and-type`・`prefix-alone-does-not-break-read-note` — permalink・type を省いたときの既定と、前置とスラッグ崩れの切り分け。
  - `indexed-without-reindex`・`search-by-type` — `bm reindex` 無しで `read-note`・`search-notes`・`--type` から引ける。
  - `second-write-note-writes-nothing`・`overwrite-loses-history`・`append-keeps-history` — 積み上げるノートの扱い。
  - `append-to-missing-creates-prefixed-note` — 無い permalink への `append` は前置したノートを黙って作り、`replace_section` は `Entity not found` で止まる（以前の手順書は `replace_section` も作ると書いていたが、LEV-192 でケースを足して違うと分かった）。
  - `append-lands-after-relations-flat-replace-section-wipes` — `append` は Relations の後ろに落ち、平らなリストへの `replace_section` は節ごと消す。
  - `inbox-replace-section-duplicates-find-replace-does-not` — inbox の形への `replace_section` の重複、`find_replace` の止まり方（2 か所・0 か所・当て先なし）、`\n` が改行にならないこと、前提が崩れた形（間に節がある・Relations の直前に空行が無い）での黙った入り方。
  - `overwrite-with-different-filename-creates-another-note` — ファイル名とタイトルが違うノートへの `--overwrite` は既存を変えず別のノートを作る（permalink の出方も）。
- **手順書の文面** —— `documents-still-say-it`。必須の記述（permalink・type の行、inbox の行頭の目印、`{folder}/{title}.md`、「`write-note` は新規専用」、前提と崩れる条件）が残っていること、旧手順（`## 未蒸留` への `replace_section`、本文や目印を二重引用符・コマンド置換で渡す形、コマンドの中の `\n`、ファイルへの heredoc 書き込み、個人の絶対パス）が戻っていないこと。

旧手順に戻すと落ちることを確かめるときは、件数ではなく**どのステップが落ちるか**で見る。落ち方は差し込み口の文字次第で変わる（本文を二重引用符で渡す旧版はシェルの構文エラーで止まり、`permalink:`・`type:` の行を消すと「差し替える箇所が無い」で止まり、そのノートを使う後続のステップも落ちる）。手順書を直したら、同じブランチでこのケースも直す。

## 公開前の追加確認

現在は GitHub Release ＋ BRAT のベータ（0.x）で、コミュニティ審査には出していない。README・LICENSE の存在だけで審査に出せるとは扱わない。製品の受入条件、実機対応、公開ライセンス、作者表記、名称・ID の重複、説明文を確認する。`manifest.version` と同じタグで必要な配布物を GitHub release に添付する（下記「リリース手順」）。

### リリース手順

ベータ配布（GitHub Release ＋ [BRAT](https://github.com/TfTHacker/obsidian42-brat)）と、将来のコミュニティ公開で共通の手順。配布物は `main.js`・`manifest.json`・`styles.css` の 3 ファイルだけで、タグは `manifest.version` と同じ `x.y.z`（先頭に `v` を付けない。[公式](https://docs.obsidian.md/plugins/releasing/release-your-plugin-with-github-actions)）。`main.js` はこれまでどおりコミットせず、Release の添付ファイルとしてだけ配る。リポジトリの public 化と LICENSE（MIT。LEV-23）は 2026-09-20 に確定し、以後この手順で 0.1.0〜0.3.7 の 11 版を公開した（0.1.0〜0.2.1 は 2026-09-20、0.3.0・0.3.1 は 2026-09-21、0.3.2〜0.3.4 は 2026-09-22、0.3.5〜0.3.7 は 2026-09-26。各版の中身は product-plan.md §5 M5）。手順の確認記録は `artifacts/lev-68-release-workflow/record.md`。

1. **バージョンを上げる**: 先に README の既知の制限のうち版を限った項目（「（x.y.z まで）」の形）と「次の版」の記述を読み、出す版で直ったものを外すか書き直す。版を限った項目は機械でも検査する（LEV-209）: README の `## 既知の制限` で始まる見出しの節（コードブロックと HTML コメントの外。見出しが無ければ失敗する）から「x.y.z まで」を拾い（括弧・空白・`v`／`Ver.` の接頭・全角数字・行の折り返しを問わない。「0.3 まで」「0.3.5-beta.1 まで」のように x.y.z でない版は比べられないので失敗する）、x.y.z が出す版より古い項目が残っていれば、`npm version` の `version` スクリプト（下記）がファイルを書き換える前に止まり、npm はコミットもタグも作らない（`package.json`・`package-lock.json` は下記のとおり `git restore` で戻す）。節が無い README も止まる。出す版で直った項目は外し、直っていない項目は外さずに版を書き直す（外すと、その版の利用者から注意書きが消える）。同じ検査を `validate`（`scripts/validate-release.mjs`）が `manifest.version` に対して行うので、版を上げたあとに README を戻した場合も 2 と `release.yml` で止まる（`npm run package` は検査しない。テスト Vault 用のビルドを README の文面で止めないため）。拾うのは文字の形なので、次の前提がある: 版を限る書き方は「まで」だけとし、「以前」「以下」「〜x.y.z」は拾わない（「0.3.2 以前の版で保存したノートは…」のように、ずっと残る注意書きにも使うため）。英字の製品名の直後に書いた版（「Obsidian 1.4.0 まで」。Mappy を除く）はその製品の版として比べないので、Mappy の版を英字の語の直後に書くと（「Undo 0.3.5 まで」）見落とし、他の製品の版を括弧で書くと（「BRAT（1.0.6 まで）」）Mappy の版として比べて止まる。版を限る項目は「（x.y.z まで）」の形で書く。「次の版」のような版を書かない記述は検査しないので、読み合わせで見る。直すなら PR（コードレビューを通す）で main に入れてから次へ進む（タグのあとに直すとタグを打ち直すことになる）。main で `npm version <x.y.z> -m "release: %s"`。npm が `package.json`・`package-lock.json` を更新したあと `version` スクリプト（`scripts/version-bump.mjs`）が `manifest.json` の `version` と `versions.json` の `<x.y.z>: <minAppVersion>` を更新して `git add` し、npm が 4 ファイルのコミットとタグ `<x.y.z>` を作る（`.npmrc` の `tag-version-prefix=` で `v` が付かない）。`minAppVersion` を上げる版では、先に `manifest.json` の `minAppVersion` を直してから実行する。`x.y.z` 以外（`v` 付き、`-beta.1` など）はスクリプトが拒否し、npm はコミットせずに止まる（`package.json`・`package-lock.json` は書き換わっているので `git restore` で戻す）。PR で上げたい場合は worktree で `npm version <x.y.z> --no-git-tag-version` を実行して 4 ファイルの変更だけを PR にし、merge 後に main で `git tag <x.y.z> <merge commit>` する。
2. **確認**: `npm run check`（`validate` が 4 ファイルの整合と README の版を限った既知の制限を検査する）。失敗したら `git tag -d <x.y.z>` で消し、直してから 1 をやり直す。main の `npm version` で上げた場合は版上げのコミットも残っているので、同じ版でやり直す前に（push 前に限り）そのコミットを `git reset --keep HEAD~1` で外す（`package.json` の版が既に上がっていると `npm version` は「Version not changed」で止まる）。PR の経路（`--no-git-tag-version`）はコミットもタグも作らないので、どちらも要らない。
3. **push**: `git push origin main <x.y.z>`。`release.yml` がタグを受け、`npm ci` → タグと `manifest.version` の一致（不一致は失敗）→ `npm run check` → `dist/mappy/` の 3 ファイルを workflow artifact に上げ → `gh release create <x.y.z> --verify-tag --generate-notes` で Release を作って 3 ファイルを添付する。`0.x` は `--prerelease`（ベータ。BRAT は pre-release も拾う。draft は push 権限のない利用者の API 応答に含まれず BRAT から見えないので使わない）、`1.0.0` 以降は通常の Release。同じタグの Release が既にあれば失敗する（タグを付け替えない。次の版を切る）。タグを push したあとに `release.yml` が check などで失敗した場合も同じで、公開済みのタグは消さず付け替えず、main で原因を直して次の版を切る（push したタグは fetch した clone に既に取り込まれうる。Release が無いので BRAT には見えない）。
4. **Release の確認**: `gh release view <x.y.z>` で添付が 3 ファイルであること、`gh run view` で `release.yml` が成功していることを見る。生成されたリリースノートは必要なら GitHub で編集する。
5. **テスターの導入（BRAT）**: リポジトリが public であることが前提（private のままなら BRAT 2.x の GitHub token 設定で Contents: Read-only の fine-grained PAT を渡す）。テスターは Obsidian に BRAT（`obsidian42-brat`。2.x は Obsidian 1.11.4 以上）を入れ、コマンド「BRAT: Add a beta plugin for testing」（設定タブでは「Add beta plugin」）にリポジトリ `hiroyaiizuka/Mappy` を入れて「Add plugin」。BRAT は semver で最大の Release（pre-release を含む）の 3 ファイルを `.obsidian/plugins/mappy/` に置く。版を固定したいテスターは「Add a beta plugin with frozen version based on a release tag」でタグを指定する。`manifest-beta.json` は不要（Release 方式では読まれない）。
6. **更新の配り方**: 1〜3 を繰り返してタグを進めるだけ。テスター側は BRAT の「Check for updates to all beta plugins and UPDATE」か、BRAT 設定の起動時自動更新で新しい Release を取り込む。

dry-run: `gh workflow run release.yml --ref <branch>`（workflow_dispatch）と、`release.yml`・`scripts/version-bump.mjs`・`package-plugin.mjs`・`validate-release.mjs` を変える PR では、同じ手順で配布物を作って workflow artifact `mappy-<manifest.version>` に上げ、Release は作らない（`release` ジョブはタグの push だけで動き、`contents: write` もそのジョブにしか渡さない）。`workflow_dispatch` は workflow ファイルが main にあるか、その workflow が一度でも走った後でないと受け付けられない（GitHub の仕様。LEV-68 では PR のトリガーで一度走らせてから `gh workflow run release.yml --ref <branch>` が通った）。専用テスト Vault は `harness:prepare` と「試用中の更新」でファイルを直接置く経路のままにし、BRAT で同じ Vault を更新しない（`harness:preflight` が root / dist と照合する対象が変わる）。

提出時には公式ドキュメントで手順を再確認し、自動レビューの指摘を解決する。sample README と差がある場合は提出時点の公式ドキュメントを優先する。[提出手順](https://docs.obsidian.md/plugins/releasing/submit-plugin)、[提出要件](https://docs.obsidian.md/community-directory/submission-requirements-for-plugins)

コードを参考にした製品の権利表示・公開方針も確認する。今回 MarkMind と Light Mindmap の製品ソースはコピーしていない。[Developer policies](https://docs.obsidian.md/community-directory/developer-policies)

### 審査要件のチェック項目

公式の[提出要件](https://docs.obsidian.md/community-directory/submission-requirements-for-plugins)・[プラグインガイドライン](https://docs.obsidian.md/Plugins/Releasing/Plugin+guidelines)・[開発者ポリシー](https://docs.obsidian.md/community-directory/developer-policies)を次の項目に分け、結果は日時・ビルドのハッシュ・根拠（バリデータ、grep、テスト名）つきで `artifacts/lev-24-readme/record.md` に残す（初回は 2026-09-19、47 項目）。提出前に同じ項目で再確認し、公式文書が変わっていれば項目を足す。lint の通過も、この表の PASS も、審査の通過を保証しない。

| 区分 | 項目 | 主な根拠 |
| --- | --- | --- |
| manifest | `id`（一意・`obsidian` を含まない）、`name`、`version` の 4 ファイル一致、`minAppVersion` の根拠（使う API と型の版）、`description` の書式、`fundingUrl` の有無、`isDesktopOnly`、不明なキー | `npm run validate`、`obsidian` 型での型検査 |
| コマンド・UI | コマンド名にプラグイン名を含めない、ID を前置しない、既定ホットキーなし、`checkCallback`、設定タブの見出し、Sentence case、`innerHTML` 不使用、インラインスタイルは動的な幾何だけ、CSS のスコープ、グローバル `app`・`activeLeaf` 不使用、サンプルコードの残り | lint（obsidianmd recommended）、grep |
| ログ・互換 | `console.*` が `src/` と `dist/mappy/main.js` にない、`node:*`／`electron`／`fs`／`process` の不使用、バンドルの `require()` が Obsidian 提供の external だけ、正規表現の後読みなし | grep、`build-meta.json` |
| ネットワーク | 自前のサーバー・テレメトリ・送信・アカウント・課金・広告なし。外部へ出る通信（ノートが参照する外部 URL の画像の表示と書き出し時の取得）を README に開示 | grep、README |
| ライフサイクル | `register*` による解除、`onunload` で leaf を detach しない、prototype 差し替えの復元、view・埋め込み・Excalidraw フックの解放。実機の E09・E25・E34 | テスト名を記録、実機は verification issue |
| 依存・配布物 | ランタイム依存とライセンス（表示義務）、`dist/mappy/` の 3 ファイルとサイズ、`main.js` をコミットしない、release のタグと添付、minify は難読化ではない | `npm run package`、`package-lock.json` |
| README・LICENSE | 保存形式、導入、基本操作、既知の制限、復旧方法、ネットワーク利用、対応環境、ライセンス、第三者コードの表示 | README、`LICENSE` |
